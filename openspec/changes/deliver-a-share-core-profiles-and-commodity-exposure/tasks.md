> 2026-09-13：用户已批准方向并完成规划更新。下面仅列尚未实现的业务任务；规范可校验不代表任务已执行。本次不启动 LLM 或生产。

## 1. M1 全 A 股通用骨架

- [x] 1.1 接入既有全 A 股证券池和有效正式年报，输出全量候选、缺资产/缺分类状态与排除原因；不复用制造六行业、固定年份或 OOS 排除名单作为生产范围。
- [x] 1.2 按 company_profile_common_core_mapping.v1 将三维评价映射到现有 overview/segment 任务，复用 business_overview_source、explicit_activity、segment_dimension、operating_revenue；实现逐维证据投影，保留六个 ChapterTask 闭集，不偷偷增加第七个任务或新 field_id；无增强包仍可执行基础任务。
- [x] 1.3 自动生成通用 Evidence，复用已有完整财务/表格事实，只对未解决语义调用 LLM；不要求逐家公司人工冻结页计划。
- [x] 1.4 核验 extract、verify、projection 和新评估均执行 report_default_group_scope；明确合法默认集团不是 unsupported promotion。先判实际事实/更窄主体错误 failed，再判仅历史Gold主体政策不一致为 gold_contract_conflict，最后应用 strictness；加入数值错误不能被冲突掩盖、母公司/分部优先、冲突 unclear、第三方 actor 不转移的回归，历史Gold和bundle不改写。
- [x] 1.5 实现三维骨架评价与记录级隔离，修复当前跨 scope 重复缺项/同页不同对象冲突；对象、期间、指标和义务不相同不能互补，业务总述不随一条 Activity 失败丢失。
- [x] 1.6 用跨制造、服务、金融等不同披露形态的实际来源/可控 fixture 证明通用任务能形成骨架；不将银行套制造业产销存，不以数百条表格值充当完整主营。

## 2. M2 持续任务和可读取交付

- [x] 2.1 复用现有持久化队列与 stage_runner 接口接入 Stage 5，明确新契约 writer/存储命名空间，证明不会调用旧语义 writer。
- [x] 2.2 实现 scope 完成即保存、lease/断点恢复、重复提交复用和源版本变化的局部 successor；停止恢复不重复已完成 provider 调用。
- [x] 2.3 将输入/规则身份、实际模型尝试、动态 token 和总预算纳入现有执行记录；传输有限重试，语义争议单列，个别失败不阻塞其他公司。
- [x] 2.4 接通预览、运行、状态、暂停、继续五个任务操作；入口只转发。发布经验证的实际任务名/参数，不把设计名冒充已有命令。
- [x] 2.5 接通按证券查询、画像模板和结构化导出；显示三维回答、时效、证据、accepted事实和缺项，数据完成一家公司即交付。
- [x] 2.6 用持久化任务集成验证重复执行、进程中断、失败隔离、更正版本和旧入口仍冻结；检查原始资产不受影响。

## 3. M3 基础商品暴露

- [ ] 3.1 先按总需求 §20.1 和通用模型主规范注册 company_profile_commodity_exposure.v1 投影字段、映射/检查状态及读写 schema，再适配商品目录到新 Activity、Measurement、Relationship；数量引用 Measurement，不从旧 Activity 数值字段复制；新投影不得混进 Stage 5 抽取枚举。
- [ ] 3.2 实现有证据的产品销售、原料投入、能源消耗等业务角色，标记程序推导及版本；数量/强度未知可空，不将角色直接变成净利润方向。
- [ ] 3.3 支持多角色并存、歧义映射单列、行情未绑定仍展示；明确“未发现明示关联”“未检查”“失败”均不等于零暴露。
- [ ] 3.4 将基础商品关联加入同一查询/画像读取，保留期间和知识时间；未授权 DCF、交易和价格敏感性消费者不获得数据授权。
- [ ] 3.5 验证铜等投入无数量/无行情仍可关联、只有行业常识不能关联、买卖并存不净额化、不同报告期间不能合并；确认一个权威写入/发布 owner。

## 4. 真实运行审查与范围切换

- [ ] 4.1 在 live 任务前记录用户运行预算、分层抽样规则、独立来源召回分母及扩大规模的质量阈值；不根据结果反向设阈值，不强制50家一轮。
- [ ] 4.2 执行一个预算有限的真实任务，逐家公司保存交付，记录全证券池分母、完成/缺资产/失败情况；不因补充信息不全整批重跑。
- [ ] 4.3 对抽样骨架、重要披露和商品角色实际核原文，报告召回、准确性、关键错误、时效、token/耗时和人工量；结构检查与语义审查分列，未评估不填0。
- [ ] 4.4 验证新契约研究 writer/reader 的启用范围、暂停恢复和回滚，再开放公司事实与基础商品关联的自动研究发布；旧 writer、DCF/交易不随之启用。
- [ ] 4.5 在新读取切换后提出旧语义数据 dry-run 删除清单及恢复方案；仅列可确认失效的旧语义/依赖，不删除正式文档或原始证据，不用全量reset阻塞新数据交付。
- [ ] 4.6 将实际缺口转为 M4 行业增强及共性缺陷待办，移除已被替代的任务入口/执行文案；运行相关测试、Review、OpenSpec校验并交付任务操作说明与提交。
