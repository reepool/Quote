## 1. Shadow cohort admission and freeze

- [x] 1.1 Add strict shadow-manifest and selection-receipt models/loaders for exactly twenty local-valid reports while preserving every historical Stage 5/OOS closed-set rule.
- [x] 1.2 Implement deterministic unseen-identity/hash exclusion and SSE/SZSE/BSE diversity checks over existing annual-report assets and first-wave industry metadata.
- [x] 1.3 Freeze the twenty-report cohort manifest and selection receipt with business shape, disclosure shape, selection reason, known limitations, local path, page count, and SHA-256 for every report.

## 2. Automatic six-chapter Evidence planning

- [x] 2.1 Implement the shadow Evidence planner by mapping existing PDF artifacts, disclosure templates, and governed section selections into continuous bounded scopes for the six existing chapter tasks.
- [x] 2.2 Add typed pre-provider failures for missing governed pages, unreadable pages, incomplete table context, unknown fields, identity/hash mismatch, and prohibited answer-bearing plan content.
- [x] 2.3 Generate and freeze all twenty Evidence plans plus a provider-free preparation audit before any semantic provider call.

## 3. Report-local shadow execution

- [x] 3.1 Admit shadow report bundles against the active hash-bound manifest at the application boundary without adding samples to historical constants or changing `Stage5RunBundle` semantics.
- [x] 3.2 Implement one shadow-batch application service that delegates report/scope semantics to the existing Stage 5 service and persists separate immutable success or typed-failure results per report.
- [x] 3.3 Add a thin operator for preparation and the single contracted semantic batch using the frozen Gemini logical profile, existing bounded gateway behavior, and no cross-model/cross-run splicing.

## 4. Metrics and source-text review package

- [x] 4.1 Aggregate cohort, preparation, execution, usability, Evidence traceability, critical-error, human-review, latency, and token metrics from immutable report outputs.
- [x] 4.2 Generate the frozen review sample containing every blocker/caveat/unresolved item plus the first accepted or legal-empty result per core chapter with source quote, page, Evidence ID, runtime target, usage restriction, and recommended decision.
- [x] 4.3 Derive `ready` or `hold` from the contracted thresholds without changing report results, semantic rules, or production authorization.

## 5. Verification and contracted batch run

- [x] 5.1 Add focused tests for legacy closed-set regression, shadow manifest integrity, automatic planning, field-contract preflight, report isolation, immutable persistence, readiness metrics, and production non-authorization.
- [x] 5.2 Run focused pytest, Ruff, strict OpenSpec validation, and a provider-free end-to-end preparation check for all twenty reports.
- [x] 5.3 After the frozen preparation audit passes, perform one external-connectivity preflight and exactly one complete twenty-report semantic batch; retain success and typed failures without targeted reruns.
- [x] 5.4 Complete the source-text review sample, publish the empirical readiness audit, and record whether a later controlled production-promotion design change is justified while keeping Stage 6 and production closed.
