## 1. Contract and immutable baseline

- [ ] 1.1 Record `stage55-closure-four-20260907-a` as the sole immutable runtime baseline and enumerate the directly affected period, qualifier, subject-strictness, and event-label Gold cases without changing the bundle.
- [ ] 1.2 Strict-validate the change and confirm that live LLM runs, Evidence-plan changes, stage six, legacy backfill, approved tables, CommodityExposure, ValueChainRole, and DCF remain out of scope.

## 2. Period semantics

- [ ] 2.1 Add focused tests for annual duration normalization, instant inventory dates, event occurrence/effective dates, and preservation of explicit narrower source periods.
- [ ] 2.2 Implement the minimum adapter-owned period normalization using the existing `period_type`, without adding a parallel period model or mutating historical records.
- [ ] 2.3 Add regression coverage proving source-native headers, Evidence anchors, knowledge time, comparison basis, and restated facts remain unchanged.

## 3. Expected-completion qualifier

- [ ] 3.1 Add provider/adapter tests for explicit expected completion on an approved continuation page and for the legal absence of completion timing.
- [ ] 3.2 Update the existing compact capacity instruction with only the generic source-copy and classification rule; do not include a target year, value, or Gold answer.
- [ ] 3.3 Preserve the exact evidenced completion expression in `source_native.qualifier` while keeping the capacity fact's report-period semantics and physical anchors.

## 4. Closed Gold adjudication rules

- [ ] 4.1 Add matcher tests for `allow_supported_non_group_refinement`, including a supported `business_segment` case and blockers for unsupported or consolidated-group promotion.
- [ ] 4.2 Implement the closed subject-strictness mode and update only the approved affected Gold metadata; do not modify its source-native value or expected subject scope.
- [ ] 4.3 Add tests for the directional Chengfei `major_asset_restructuring_effective` to runtime `equity_transfer` decision with exact anchor and `2025-01-06` conditions, plus negative project-launch, commitment, date, and anchor cases.
- [ ] 4.4 Implement the single auditable event-equivalence decision without introducing broad event aliases or changing either event label.
- [ ] 4.5 Prove the Chengfei sales coverage remains `gold_contract_conflict` and missing facts, Segment structure, or different physical anchors remain failed.

## 5. Offline verification and handoff

- [ ] 5.1 Run the focused company-profile provider, benchmark, model, and acceptance-policy tests plus Ruff for changed Python files; fix only blockers within this change.
- [ ] 5.2 Generate a new offline benchmark identity from the immutable closure bundle with no extract, repair, verify, PDF, or network invocation.
- [ ] 5.3 Audit every changed and unchanged Gold outcome, confirm the authority bundle is byte-for-byte unchanged, and retain `research_slice_usable` with `production_authorization=not_authorized`.
- [ ] 5.4 Update only the directly affected current semantic documentation, run final strict OpenSpec validation, review the scoped diff, and prepare the completed change for archive without starting stage six.
