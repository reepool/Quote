## 1. Freeze external validation

- [x] 1.1 Bind the predecessor sandbox failure, exact two scopes, source and implementation hashes, Gemini route, unchanged budgets, sole external validation ID, output path, and transfer authorization target.
- [x] 1.2 Add a change-local thin operator that validates the frozen contract before provider construction and delegates semantics to the existing Stage 5 service.

## 2. Provider-free verification

- [x] 2.1 Add focused tests for exact admission, predecessor/source drift, output reuse, and unchanged research-only boundaries.
- [x] 2.2 Run focused tests, Ruff, strict OpenSpec validation, and source hash checks before the external request.

## 3. Sole external validation

- [x] 3.1 Launch the provider-bearing operator outside the sandbox from the start under the sole new identity; do not tune, substitute, or rerun.
- [x] 3.2 Persist and evaluate both case-local results, including accepted rows, task completeness, and recurrence of the targeted failures.

## 4. Closeout

- [x] 4.1 Record whether a separate twenty-report replay is justified, retain `production_authorization=not_authorized`, and verify both prior inputs remain unchanged.
- [x] 4.2 Commit and push only this change, leaving task-start worktree changes untouched.
