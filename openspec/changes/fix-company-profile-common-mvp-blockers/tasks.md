## 1. Bind the observed common defects

- [x] 1.1 Record the seven reviewed error rows with their persisted extract and verify model lineage and classify the three shared defect families.
- [x] 1.2 Add provider-free regressions for substantive overview boundaries, material/segment legal-empty Evidence ownership, and source-native event labels.

## 2. Retire obsolete replay compatibility

- [x] 2.1 Remove the Evidence-role and completed current-MVP replay proof validators and generic correction-audit admission for their historical schemas while preserving artifacts.
- [x] 2.2 Remove both one-shot replay contracts, CLI modes/options/branches, mode admission rules, fixtures, and tests; confirm no active caller remains.

## 3. Fix shared semantic behavior

- [x] 3.1 Implement isolated BusinessOverview admission/narrowing for the four reviewed source shapes without company-specific identifiers.
- [x] 3.2 Require owner-valid disclosure classes for material-input and segment legal-empty coverage.
- [x] 3.3 Clear unsupported canonical English BusinessEvent source-native names while retaining evidenced event semantics.

## 4. Fix bounded routed failures and trace lineage

- [x] 4.1 Reserve a bounded portion of the remaining logical deadline when another configured pool hop is possible, preserving one deadline and finite hops.
- [x] 4.2 Persist safe selected-profile, source-label, failover-count, and physical-attempt lineage in successful and failed Stage 5 traces.
- [x] 4.3 Add controlled gateway and Stage 5 tests proving a stalled source can fail over and terminal failures retain model lineage without content or secrets.

## 5. Validate and close

- [x] 5.1 Run focused gateway/company-profile suites, Ruff, `git diff --check`, and strict OpenSpec validation without a live LLM or batch rerun.
- [x] 5.2 Review only blocking defects, verify historical bundles and task-start workspace changes are untouched, then commit and push only this change.
