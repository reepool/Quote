## 1. Freeze the segment-financial failure contract

- [x] 1.1 Record provider-free fixtures for differing partition Evidence, harmless metadata drift, real subject/period conflicts, and duplicate metric cells.
- [x] 1.2 Record cross-page dimension, cost-component, incidental-owner, explicit one-segment, and legal-empty positive/negative controls from the reviewed shadow cases.

## 2. Repair the existing Stage 5 segment path

- [x] 2.1 Reconcile segment partitions by source-backed dimension/label, retain the stable approved Evidence union, and keep real metadata/cell conflicts blocking.
- [x] 2.2 Validate dimensions from the controlled table scope while retaining row-label and numeric-cell Evidence checks.
- [x] 2.3 Close the reviewed segment-owner false positives without weakening explicit table, one-segment, legal-empty, or cost-table-with-dimension controls.
- [x] 2.4 Preserve the exact bounded local merge/normalization cause under the existing `candidate_schema_invalid` classification.

## 3. Prove the bounded repair

- [x] 3.1 Add focused provider-free tests for every frozen positive and negative fixture and assert zero provider calls.
- [x] 3.2 Run the focused Stage 5/shadow tests, Ruff on touched Python files, `git diff --check`, and strict OpenSpec validation.

## 4. Close for review without replay

- [x] 4.1 Publish a provider-free closure audit mapping the reviewed blockers to fixed, intentionally rejected, or still-unresolved outcomes and retain `production_authorization=not_authorized`.
- [x] 4.2 Review only blocking defects, commit and push isolated changes, and leave any later twenty-report replay to a separate reviewed proposal.
