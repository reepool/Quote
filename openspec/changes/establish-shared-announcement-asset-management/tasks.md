> Completion evidence gate: the existing checked boxes record implementation progress only. Until task 1.8 migrates/replaces the existing section-level registry with and validates the version 2 bidirectional requirement-leaf/normative-clause registry, no checked task SHALL be treated as proof of requirements coverage and no additional completion claim SHALL be accepted. The existing registry is only the immutable-ID migration baseline. Task 1.8 SHALL retroactively register every already-checked task or reopen it when exact requirement/spec/task/owner coverage cannot be established.

## 1. Contracts And Configuration

- [x] 1.1 Define business-neutral announcement, attachment, blob, attachment-observation, effective-annual-report current projection, immutable effective-decision/replacement history, internal operation versus caller subscription/request, deletion/recovery, recovery-pair closure, backup recovery-journal, change-event/checkpoint, and consumer-processing models with stable schema versions; clean/migrated schema tests SHALL assert every minimum field and constraint in the canonical schema requirement, including raw/normalized publication evidence, attachment hints, adoption/verification/backup evidence, effective decision/last-checked evidence, append-only predecessor/replacement observation/blob/outbox linkage, operation bounds/progress, internal operation status without `expired`, caller projection expiry/tombstones, versioned null/not-applicable stages, reserved pair id plus immutable manifest/separate append-only closure, recovery-journal ordering/integrity/truncation watermarks, and recovery-retention-pin `blocks_primary_unlink`/`required_set_hold` transition state. A migrated legacy manifest with a catalog snapshot watermark or hint SHALL preserve/receive a stable reserved pair id but SHALL NOT fabricate a recovery-pair closure before fresh bidirectional verification.
- [x] 1.2 Add an independently enabled annual-report asset configuration section and config-template defaults covering module/scheduled enablement, active exchanges and instrument type/status, `dry_run`, archive/temp/quarantine roots, providers/routes/normalized categories, classifier and eligibility policies, bootstrap scope and filing-season bounds, targeted-repair request/instrument/elapsed bounds, initial lookback, versioned `universe_refresh_cadence`, independently configured `master_data_max_age` and `listed_security_census_max_age`, indeterminate and overdue-missing policy, daily timezone/overlap, `reconciliation_lookback_days`, `reconciliation_cohort_size`, `reconciliation_max_cycle_days`, `missing_repair_cohort_size`, page/request/window/instrument/byte/elapsed limits, download/per-source concurrency and rate limits, warning/stop/free-space and temporary-byte gates, per-attachment and per-task planned/actual byte limits, byte reservations and unknown-length budgets, `wait_seconds` default/maximum, lease TTL/heartbeat/safety grace, retries, audited capacity-override policy, backup target/freshness and recovery-journal retention/integrity policy, runtime `max_unprotected_bytes`/`max_unprotected_age` with accumulation origin and successful-backup reset/unblock semantics, version 1 provisional-result policy/version, consumer dependency policy, rollout gates, and the per-job defaults: latest backfill `manual_only=true` with no cron, daily update and backup cron-plus-manual but `enabled=false`, and integrity audit manual/low-frequency and read-only by default; add JSON/schema parity tests proving production config, template, and schema round-trip every required field, reject missing/invalid/unsafe values (including non-positive reconciliation/repair bounds and zero/boundary unprotected thresholds), preserve normalized configuration fingerprints, load with these exact fail-safe defaults, survive restart with correct unprotected accumulation, clear the blocker only after verified backup closure, and cannot register a latest-backfill cron.
- [x] 1.3 Validate that configured archive, temporary, quarantine, and backup paths cannot escape their permitted roots or silently resolve a missing NAS mount onto local storage.
- [x] 1.4 Add clean-database schema creation and migration tests that snapshot and compare the real existing business-profile and financial-fact tables, indexes, constraints, and representative rows before/after migration, rather than relying on a synthetic placeholder table.
- [x] 1.5 Add import/startup tests proving service, DataManager, API/router, and scheduler registration perform zero provider scans, downloads, operation dispatch, migration moves/links/quarantine, or deletions while still allowing local schema/configuration validation.
- [x] 1.6 Define principal/consumer-scoped operation subscriptions and opaque query handles separately from globally single-flight asset operations, including schema version, ownership, idempotency fingerprint, authorized projection, and consumer-continuation links.
- [x] 1.7 Add config/template validation for `.part` maximum age/bytes and safety grace, quarantine warning/hard age/bytes and audited cleanup policy, plus deterministic degraded/blocked readiness mapping for invalid sidecars and threshold crossings.
- [x] 1.8 Before further implementation completion claims, mint immutable unique ids for every independently testable requirements-document leaf and every normative OpenSpec scenario or independently testable SHALL in `evidence/traceability_registry.json`. Persist normalized clause text hashes and auditable source locators; bind requirement leaves and spec clauses bidirectionally to exact implementation tasks and accountable owners rather than inheriting one section/task set for an entire Requirement heading. Preserve rename/relocation identity through append-only alias history verified against a prior registry baseline. Split or reject lines containing multiple independent SHALL obligations. Add schema/CI mutation tests that reject duplicate/orphan ids, an unmapped leaf added under an existing heading even after the whole-file fingerprint is updated, parent-only mappings, requirement-wide Cartesian task mappings, missing task/owner links, fabricated aliases, and checked tasks without exact registered coverage; release evidence and sign-off remain completed in 11.7.
- [x] 1.9 Validate every dynamic archive, temporary, quarantine, and backup path segment against a declared canonical type allowlist before path resolution or filesystem mutation, including exact lowercase SHA-256 names and governed operation/alias/source identities; reject empty, `.`, `..`, absolute, separator-containing, encoded-traversal, control-character, and non-canonical segments with zero file or catalog mutation.
- [x] 1.10 Add a CI/release test that directly loads the promoted `evidence/traceability_registry.json` and validates current requirements/spec/task fingerprints, schema, the explicitly pinned prior-v2 chain, exact bidirectional coverage, multi-SHALL dispositions, and checked-task coverage with `require_complete=true`; candidate-generation tests alone SHALL not satisfy this gate.

## 2. Canonical Repository And Classification

- [x] 2.1 Implement repositories for source-qualified announcements, attachments, content blobs, immutable attachment observations, effective annual reports, durable operations, replayable change events, deletion audit, and optional consumer-processing links, preserving first/last observed and raw metadata identity.
- [x] 2.2 Implement deterministic idempotent upsert, unique-key, transactional retention-pin (including managed alias owner/hash/expiry/cutover metadata), lease, lease-generation/state-version-fenced per-source item cursor and range `covered_until`, acquisition work fingerprint, and operation-transition behavior with concurrency tests; database pins, never `st_nlink` or a hand-maintained count, SHALL be deletion truth. Prove stale discovery workers cannot regress newer cursor/gap/completion state and incompatible route/integrity/bound fingerprints cannot share active work.
- [x] 2.3 Implement the versioned attachment-level formal annual-report classifier for originals, complete corrections, summaries, translations, visual editions, audit/inquiry/briefing material, periodic reports outside V1, notices, and unresolvable identities.
- [x] 2.4 Implement the foundational fiscal-year/report-period extraction, versioned `as_of`/listing/disclosure-calendar/provider-coverage search-bound policy, and deterministic latest-effective winner selection over committed observations, including basic multiple-correction ordering, withdrawal/silent-update observation registration, overdue coverage gaps, fail-closed ambiguity diagnostics, and the same-source/same-legal-chain/same-time/same-hash stable identity tie-break with reverse-discovery-order invariance. Cross-source equivalent-filing evidence/projection is completed only by 2.7, and already-serving-winner conflict/provisional `pending_correction` governance is completed only by 2.8; checking 2.4 SHALL NOT be interpreted as evidence that those later contracts are complete.
- [x] 2.5 Add immutable observation `version_available_at`/time-source/precision semantics, exact withdrawal-target evidence, and fail-closed same-source timestamp-tie handling; prove knowledge cutoffs cannot see later silent-update bytes or retroactive withdrawal state.
- [x] 2.6 Enforce and test one versioned classification vocabulary across providers, migration, shared assets, APIs, and consumer adapters: document family is orthogonal to `original|correction` variant and notice-only correction evidence is never promoted as a full report; register a non-annual test document family and a neutral architecture-contract test consumer to prove generic identity/blob/acquisition boundaries are reusable without consumer/provider downloader, scanner, archive-layout, revision-store, business-profile, or broker imports, while leaving production semiannual enablement outside V1.
- [x] 2.7 Persist a stable equivalent-source-filing evidence set for cross-source same-hash/mirror candidates and a versioned discovery-order-independent canonical projection, including policy version/evidence-set hash in effective and consumer lineage; test reverse discovery order and later mirror arrival without legal-identity merge or silent consumer-identity drift.
- [x] 2.8 Govern a newly discovered conflict after a winner is already serving: transition the decision to ambiguous without deleting prior bytes or creating a predecessor deletion intent, expose the prior asset only as provisional, suppress new default-effective consumer processing, project existing default-effective results as `stale/pending_correction` and exclude them from current-required DCF while preserving out-of-scope exact/cutoff results, persist the version 1 policy/version, and converge through a transactional decision/outbox after resolution; test both no-prior-winner and already-serving-winner sequences plus policy-version migration.
- [x] 2.9 Implement the append-only effective-decision/replacement-history repository and constraints independently from the mutable current-effective projection. Persist scope, predecessor/replacement legal filing, attachment observation, asset/blob, decision/policy version, reason, activation time, and outbox event key; allow nullable replacement identity only for the explicit `withdrawn_without_replacement` decision kind with a no-winner tombstone; prove original-to-correction-1-to-correction-2, same-hash distinct filing, withdrawal with/without fallback, migration, append-only uniqueness, and paired-restore reconstruction cannot overwrite or orphan an earlier transition.
- [x] 2.10 Add a neutral future-document-family acquisition-policy contract that round-trips and fingerprints `metadata_only`, bounded explicit-universe proactive acquisition, and independently governed full-market proactive acquisition together with family-specific effective-version and retention policy. Prove all three scopes with a non-production test family while production acquisition remains disabled and no non-annual attachment is downloaded by version 1 maintenance.

## 3. File Acquisition And Lifecycle

- [x] 3.1 Build the shared service on existing `research.announcements` discovery and governed attachment retrieval contracts without adding provider-specific transport logic; preserve ordered primary/fallback route attempts, failure diagnostics, selected route, and source-qualified announcement/attachment identity for each discovery.
- [x] 3.2 Implement attachment-scoped single-flight leases, bounded retries, `.part` writes, PDF signature and size checks, SHA-256 verification, flush, atomic publication, and post-rename reopen/length/hash verification; revalidate permitted root and approved mount identity at operation start and immediately before every publish/link/move/unlink mutation, with remount-race tests. When a trusted Content-Length or streamed byte count exceeds the configured limit, stop immediately, remove or governed-quarantine the temporary file, reconcile reservations against actual `.part`/quarantine bytes, and prove no attachment version or canonical blob becomes valid.
- [x] 3.3 Implement local integrity levels, corruption/missing-file handling, safe quarantine state, and bounded reacquisition.
- [x] 3.4 Implement the foundational correction activation and durable `planned -> deleting -> deleted|failed` deletion-intent state machine, basic consumer invalidation/change intents, retention pins, and append-only audit while preserving the old effective file on any failed replacement precondition. Durable outbox delivery, consumer checkpoints, selective cutoff invalidation, and policy-gated reprocessing remain tasks 3.8 and 7.4; backup/recovery-manifest handoff plus safe primary unlink remains task 3.11; checking 3.4 SHALL NOT be interpreted as evidence that those later lifecycle contracts are complete.
- [x] 3.5 Add crash-injection and concurrent-reader tests around database commit, NFS unlink, audit finalization, shared blobs, aliases, and expired deletion/read/processing leases, including owner/heartbeat/generation compare-and-swap and safety-grace races. Before `deleted` finalization, revalidate the operation-captured approved mount identity/source/read-write state and confirm absence on that same mount; a changed or unverifiable mount SHALL retain intent status `deleting`, project a blocked finalization condition through readiness/cleanup, and SHALL NOT treat fallback-path absence as deletion evidence. The race matrix SHALL explicitly include unchanged source/export with a changed filesystem identity, the same identity becoming read-only, and a post-unlink fallback mount on which the target path also happens to be absent; none may write a `deleted` audit record.
- [x] 3.6 Implement storage warning/stop/reserve, atomic concurrent byte reservations, unknown-size budgeting, and annual-report stream limits so metadata discovery continues when scheduled attachment acquisition is blocked. Add authorization/audit tests proving a capacity override is explicit, bounded to one operation and target, cannot bypass path/mount identity, PDF/integrity, per-attachment limit, or backup/deletion safety gates, and leaves no global configuration mutation; requests without the override scope or outside its bound remain blocked with zero provider/file/catalog mutation.
- [x] 3.7 Serialize effective-version activation at `instrument + fiscal_year`, reselect the winner transactionally, and test reverse completion of concurrent corrections, stale compare-and-swap, withdrawal races, and deletion-intent safety.
- [x] 3.8 Enforce the activation boundary explicitly: atomically commit the current effective projection or explicit no-winner tombstone, append-only decision/replacement record, replayable change-event outbox, and a deletion intent only when predecessor bytes are a distinct physical-unlink target; a same-hash distinct legal filing transition SHALL update legal lineage/references with a versioned `not_applicable_shared_blob` audit outcome and SHALL NOT create a physical deletion intent for the winner's shared blob. Any pre-commit failure keeps the predecessor decision, while post-commit delivery, invalidation, backup, or unlink failure keeps the correction or withdrawal decision effective and resumes durable downstream work without rollback. Test crashes before commit, before delivery, and before consumer checkpoint plus original-to-correction-1-to-correction-2, same-hash distinct legal filing, withdrawal with fallback, withdrawal without replacement/null identity, append-only uniqueness, and outbox-key linkage.
- [x] 3.9 Persist `.part` owner/generation/age/byte evidence, reconcile it independently from byte reservations, add lease-safe stale-part recovery, and govern quarantine age/byte readiness plus operator-authorized audited cleanup with heartbeat-generation race and crash tests.
- [x] 3.10 Implement and test the retry taxonomy: transient provider/attachment/NAS failures use bounded backoff with `next_retry_at`; identity, path, PDF, persistent hash, ambiguity, and unsplittable-window failures require blocked/operator handling; storage reserve failures remain blocked without high-frequency retry; max-attempt `exhausted` is an attachment/discovery retry-item substate projected as parent operation `blocked` with `retry_exhausted`, and only reopens through a new observation, due repair, or audited repair.
- [x] 3.11 For a distinct physical predecessor, create the deletion intent and recovery-retention pin at activation with a reserved `recovery_pair_id`; same-hash legal transitions from 3.8 SHALL bypass this physical-unlink workflow. The backup required-blob enumerator SHALL include every predecessor named by a `planned|deleting` intent before its immutable recovery manifest exists. After predecessor and replacement backup succeeds, persist an immutable indefinitely-active version 1 manifest binding legal/attachment identity, prior path/hash, replacement, backup objects, file-manifest watermark, and reserved pair id; then create a recoverable catalog snapshot containing that manifest and append a separate verified recovery-pair closure binding pair id, catalog snapshot identity/hash, and file watermark. Only that closure may authorize compare-and-swap conversion from primary-unlink protection to permanent non-primary-blocking `required_set_hold`; the immutable manifest SHALL never be rewritten to close the pair. For `withdrawn_without_replacement`, use the same reserved-id -> predecessor backup -> immutable `withdrawal_tombstone` with file watermark -> no-winner catalog snapshot containing it -> append-only closure -> pin-CAS sequence, require no replacement Blob, and prohibit a fabricated replacement identity. Prove crash recovery and mount-identity-safe finalization at every boundary, continuous primary-or-required-set protection, closure uniqueness, recovery hold retention after unlink, and no primary unlink without the recoverable pair, required blobs, and released non-recovery/deletion-blocking pins.

## 4. Existing Archive Adoption

- [x] 4.1 Build a read-only inventory tool for business-profile and broker annual-report manifests and files that reports valid, missing, corrupt, duplicate, conflicting, superseded, and adoptable entries.
- [x] 4.2 Register valid existing annual-report files in shadow state using source identity, report period, PDF signature, length, and SHA-256 without redownloading or moving them.
- [x] 4.3 Reconcile latest-effective decisions against the existing `AnnualReportAssetCatalog`, business-profile manifests, and broker source manifests, and require explicit conflict resolution before cutover.
- [x] 4.4 Add a separately gated convergence tool whose default dry-run emits a per-file manifest/hash allowlist and plan with zero database, file, or watermark mutation. Only an explicitly approved per-file execution may persist a versioned `manifest_kind=legacy_path_rollback` entry in the common recovery manifest mapping `legacy_path -> content_hash/shared_asset/consumer` before approved cleanup. Exclude derived files, semiannual reports, fiscal-year files not selected by the current operation's explicit adoption/cleanup allowlist, orphans, and conflicts in both modes.
- [x] 4.5 Probe NFS mount identity and link/rename behavior, then create verified links or copies only after shared reads, database pin/reference checks, independent-failure-domain backup checks, and reconciliation succeed; test alias metadata and a preflight-to-mutation remount race fail closed without touching a fallback path or advancing watermarks.
- [x] 4.6 Prove the inventory-only mode performs zero network, move, link, quarantine, or delete operations and that latest-only bootstrap never deletes valid older-fiscal-year files merely because they are outside its target.
- [x] 4.7 Adopt verifiable older-fiscal-year local reports for zero-network period-specific reuse without counting them toward latest-only coverage or triggering adjacent historical network backfill, applying normal correction governance within each adopted period after activation/outbox/recovery/deletion lifecycle tasks are complete.
- [x] 4.8 Add and test an asset-adoption promotion gate that excludes shadow records from production/effective/bootstrap use until reconciliation passes, then permits shared bootstrap/daily/local-first reuse independently from business-profile or broker consumer cutover; require shared custody or canonical convergence for legacy paths and fail local reads closed on external overwrite/delete races.
- [x] 4.9 Add optional zero-attachment-download orphan reconciliation using official metadata or audited operator mapping, promoting only fully resolved source/attachment/instrument/period/classification/length/hash evidence into shadow while preserving ambiguous orphans unchanged.
- [x] 4.10 Add dry-run and execution-mode mixed-archive tests proving excluded semiannual, non-allowlisted fiscal-year, derived, orphan, and conflicting files retain path, bytes, hash, mtime, and permissions and are never touched, chmodded, moved, linked, quarantined, or rewritten; explicitly prove a 4.7-selected older fiscal-year asset is adoptable but does not count toward latest-only coverage.
- [x] 4.11 Make the legacy annual-report base-root registry versioned and configurable while defaulting exactly to `data/filings/business_profile` and `data/filings/financial_statements/broker_risk_control`, with versioned templates `business_profile/{fiscal_year}/{exchange}/` and `broker_risk_control/{exchange}/{symbol}/` mapped to canonical instrument identity; require coverage of both roots, exclude `business_profile/derived/`, broker semiannual files, unrelated years and document families, report unknown `data/filings` directories read-only as out of scope, and prove no directory is recursively adopted or cleaned without an explicit root and classification policy. Negative tests SHALL prove directory `{fiscal_year}/{exchange}` and `{exchange}/{symbol}` segments agree with the canonical instrument/report identity derived from the manifest and filename; cross-directory mismatches remain out of scope or blocked rather than adoptable.

## 5. Latest-Only Historical Bootstrap

- [x] 5.1 Implement a versioned A-share eligibility policy and snapshot the current active SSE, SZSE, and BSE universe with listing metadata, master-data version, policy version, paired census snapshot id, and durable per-instrument coverage state; include main-board/STAR/ChiNext/BSE, ST, and suspended-active stocks while explicitly excluding B shares, depositary receipts/CDRs even when source master data loosely types them as stock, and non-stock security types. A local-master-only snapshot MAY support bounded work but SHALL NOT independently produce full-market completion/readiness before 5.7 supplies an acceptable paired census snapshot.
- [x] 5.2 Implement bounded market-wide annual-category discovery over current and prior filing seasons with adaptive date-window partitioning and durable progress.
- [x] 5.3 Select, publish, persistently retain, and count coverage only for each instrument's latest available fiscal-year winner, preferring the newest verified complete correction and reusing adopted files; when cross-source equivalence cannot be established from trusted provider evidence, any bounded temporary candidate verification is policy-authorized, non-effective, reservation-governed, and cleaned/quarantined without creating a second canonical blob, while immutable attachment-observation/version evidence retains its hash, length, retrieval proof, policy version, and cleanup outcome.
- [x] 5.4 Implement rotating targeted repair for uncovered instruments with fixed `as_of`, deterministic candidate/due/earliest fiscal-year bounds, listing and provider-coverage evidence, expiring confirmed-missing evidence, overdue-but-older-asset-usable state, retry states, and restart-safe checkpoints.
- [x] 5.5 Add bootstrap reports and tests proving latest-only canonical publication/retention/coverage, separately metered bounded candidate-verification bytes and immutable non-winner evidence, attachment-level correction precedence, cross-source conflict blocking, January and April-30 boundary behavior, fixed-cutoff exclusion of later announcements/observations/silent updates/withdrawals across retry and resume, post-period listings, delayed filings, zero redownload on resume, and master-data-only terminal-state behavior. Every per-instrument row SHALL separately report latest winner fiscal year, `bootstrap_asset_status=available|confirmed_missing|retryable|blocked`, expected-period coverage, and terminal/retry evidence; an evidence-backed no-report outcome has no winner/blob, and a provisional predecessor under an unverifiable newer correction remains `retryable|blocked` and receives no `available|current` latest-only credit. Tests SHALL prove this bootstrap vocabulary never leaks into the front-facing `asset_availability=local_valid|metadata_only|missing|ambiguous|corrupt|superseded|blocked` field; targeted repair SHALL traverse candidate fiscal years newest-first, continue past a complete empty not-yet-due year, refuse to select an older year when any newer scope is incomplete/retryable/blocked/ambiguous, and stop before downloading older attachments after a valid winner is committed; an audited query-equivalent complete-empty fallback advances only its own source-qualified cursor and MAY satisfy the independent route-level required-coverage projection needed for `confirmed_missing`, while the failed primary cursor and diagnostic gap remain unchanged; and a non-equivalent fallback preserves both the primary and route-level gap and cannot produce confirmed-missing/full-market success. The full-market `success` claim additionally remains gated on 5.7 and SHALL not be marked complete before that paired census evidence exists.
- [x] 5.6 Add master-data freshness and `eligibility_indeterminate` governance with last-complete-snapshot fallback, plus orthogonal `bootstrap_asset_status`/expected-period coverage and default non-circular `overdue_missing` daily-readiness behavior.
- [x] 5.7 Add an independent approved SSE/SZSE/BSE listed-security census with source/query-boundary/completeness-watermark/version/time/raw-hash evidence, reconcile it to the local active-universe snapshot, persist the paired census snapshot id on every effective coverage snapshot, retain the last acceptable pair on partial refresh, and test census-only new listings, missing exchanges, identity mismatches, missing or conflicting currency/security-type/exchange/active-status/listing-identity fields, plus every fresh/stale combination of the independently configured master/census maximum ages blocks or permits a full-market completion claim correctly without silently shrinking the denominator.
- [x] 5.8 Implement deterministic newest-first fiscal-year traversal for targeted bootstrap repair from candidate upper year through the earliest searchable year: continue past a completely scanned empty not-yet-due year, stop without older-year selection on any incomplete/retryable/blocked/ambiguous newer scope, stop older attachment acquisition after committing a valid winner, and permit `confirmed_missing` only after complete empty coverage through the earliest bound with listing/source evidence; cover restart checkpoints at every transition without repeating completed year scopes.
- [x] 5.9 Bind bootstrap discovery, targeted repair, retries, resume, candidate classification, observation visibility, winner selection, and coverage credit to one persisted project-timezone `as_of`/inclusive cutoff and query fingerprint; exclude post-cutoff announcements, attachment observations, silent updates, and withdrawals from that bootstrap result while registering them for daily handling, and reject incompatible resume checkpoints. Test a long-running bootstrap across the cutoff with official and first-observed availability-time variants.
- [x] 5.10 Persist and restart-reload field-complete `confirmed_missing` evidence: required source/route scopes; per-scope exchange/category/query bounds, successful-empty watermark, and page/subscope completion; listing evidence; bootstrap `as_of`/cutoff; confirmation/expiry times; route-capability/query/classifier/eligibility fingerprints; and stable source/checkpoint/route-equivalence evidence references. Missing evidence SHALL block terminal credit, while expiry or relevant fingerprint drift SHALL reenter bounded repair.

## 6. Independent Daily Update

- [x] 6.1 Add independent `annual_report_asset_latest_backfill` and `annual_report_asset_daily_update` scheduler jobs with no business-profile or broker module dependency; define a project-timezone daily cadence (at least one eligible run per calendar day by default), persisted cadence/cutoff/overlap fingerprint, and bounded missed-run catch-up from the last committed watermark.
- [x] 6.2 Implement cursor-driven market discovery keyed by source/exchange/category/scope/config fingerprint, a shared versioned provider route-capability matrix that excludes unsupported source/exchange Cartesian combinations, separate provider item cursors and range `covered_until`, lease-generation and expected-state-version fencing, a default three-calendar-day overlap, fixed project-timezone cutoff, explicit boundary semantics, provider-capability conversion from internal timestamps to date-only `YYYY-MM-DD` parameters where required, same-incomplete-window-only page/item cursor reuse, complete-window coverage commits including success-empty windows, and adaptive partitioning. Test worker A lease expiry, worker B advancement, and worker A late commit without watermark regression or newer cursor/gap/completion overwrite.
- [x] 6.3 Preserve discovered metadata across attachment failures and complete dense single days through durable page/subscope continuation under a fixed cutoff; retain prior range coverage only for incomplete discovery, keep primary/fallback source gaps independent under route policy, and expose an explicit blocker when no safe completion path exists.
- [x] 6.4 Proactively download newly effective originals and corrections during the V1 daily job, execute governed period-scoped correction/withdrawal handling including withdrawal-only metadata and same-batch original/correction failure semantics, report batch outcome `partial|blocked` for an affected scope with an unverifiable correction while retaining a legal predecessor only as `provisional` and its correction attachment as `retryable|blocked`, and run separate bounded cohorts for missing coverage, long-publication-window reconciliation, oldest-first managed-period reconciliation, and same-identity silent-byte verification with persisted fairness/retry state. The provider capability matrix SHALL select trustworthy conditional/version signals where available or bounded temporary hash verification otherwise, and unsupported detection SHALL remain an explicit readiness limitation.
- [x] 6.5 Before each daily run, refresh or revalidate the paired active-universe master/census snapshot under a versioned `universe_refresh_cadence`; persist attempted/effective refresh time and paired snapshot id so new listings enter repair, delistings leave the coverage denominator without deleting assets, census differences remain explicit, expired last-good pairs cannot claim full-market completion, and older-fiscal-year corrections stay period-scoped.
- [x] 6.6 Emit persisted, reloadable stage logs, metrics, durable job results, affected-asset events, and report-schema contract tests covering all daily fields (excluded/ambiguous counts, adoption/failure counts, new effective and dereferenced counts, silent-update verification/limitation counts, repair cohort, per-stage timings) and all bootstrap fields (universe snapshot identity, winner counts, duplicate-content counts, completed/incomplete windows, resume identity, total bytes, free space, and unprotected bytes), plus tests for a dense day containing exactly 1,500 distinct records with a provider hard page/continuation cap of 600, asserting exactly 1,500 unique ids and no parent-cursor advance before every child commits; consecutive success-empty windows, equal/future timestamps, seven-day-late and years-late managed-period corrections, same-URL changed and unchanged bytes, provider routes with and without trustworthy version signals, ordered primary/fallback attempt history with route decision, failure diagnostics, selected source-qualified identity, stale-worker generation fencing, primary/fallback partial coverage, unsupported route exclusion, cursor/config fingerprints, canonical or explicitly mapped bootstrap-to-daily no-gap handoff, non-annual metadata with zero V1 prefetch, and scheduled/manual/API overlap; record provider-delay observations and overlap calibration evidence before scheduled enablement.
- [x] 6.7 Separate discovery-window completion from attachment retry so a fully discovered window may advance its cursor while failed attachments remain durably queued, and prove overlap rediscovery cannot reset completed, not-yet-due, exhausted, blocked, or operator-held observations without a new version, due retry, or audited repair.
- [x] 6.8 Add one authenticated durable scheduler command service used by cron, operator CLI, and any operator API for bounded start, status/history, cooperative stop, and resume; use the operation id as run id, reuse only duplicate scopes with the same versioned acquisition work fingerprint, resume the same id with an incremented attempt/generation, audit operator/service principals and config versions, and prove disabled cron does not disable local reads or on-demand ensure. Negative tests SHALL prove missing operator scope, unavailable identity boundary, over-broad scope, or incompatible active route/integrity/bound fingerprints fail before incompatible work reuse with zero unintended provider/file/watermark mutation, while cron service-principal identity, config version, actor, and action remain auditable.
- [x] 6.9 Version and persist consumer dependency policy for post-success/partial daily outcomes plus the V1 `pending_correction` default, pass only completed final affected asset ids to declared consumers, suppress new default-effective processing for provisional assets, project existing default-effective results as stale and DCF-ineligible while preserving out-of-scope exact/cutoff results, and report every skipped scope and policy decision without embedding consumer download logic in the asset job. A disabled, enqueue-failed, parser-failed, or checkpoint-lagging consumer SHALL affect only its own continuation/readiness and SHALL NOT regress committed asset cursors, effective decisions, operation outcomes, asset-layer readiness, or other consumers' access.

## 7. Local-First Consumer Service

- [x] 7.1 Implement lookup and `ensure_annual_report` for instrument/fiscal-year and exact source-filing requests with canonical `source_announcement_id` plus a versioned `filing_id` compatibility alias, versioned `allow_network`, `integrity_level`, configured `wait_seconds` default/maximum, `knowledge_cutoff`, attachment/hash/observation pins, and version 1 retention policies, returning the resolved observation identity and metadata-only unavailable state when bytes are not retained. An authorized network-enabled exact selector absent from the local catalog SHALL use only a provider-supported exact-id lookup or bounded source-qualified instrument/category/date discovery capable of proving that requested identity; it SHALL never substitute another same-period filing. `wait_seconds=0` SHALL return immediately; a positive value SHALL wait no longer than the configured maximum, return HTTP 200 if the immediate projection completes inside that bound, or return HTTP 202 with the same durable handle on timeout without cancelling or duplicating work. Ordinary ensure SHALL network-acquire only the metadata candidate selected by current policy as the prospective winner; metadata-only SHALL become current/effective only after successful validation and transactional activation.
- [x] 7.2 Return verified local hits with zero provider calls, acquire an eligible metadata-only prospective-winner candidate and activate it only after verification, run bounded instrument discovery for absent period metadata, run bounded source-qualified discovery for absent exact-filing metadata, fail closed for ambiguous or incomplete requests, and let only an authorized internal DataManager/service exact-observation handle return an exactly matched retained non-winning, superseded, withdrawn, or historical byte that passes the requested integrity policy; public ensure returns `retained_internal_only` or metadata-only `local_content_unavailable`, and the public asset-id content route remains 410. Local-only generic ensure follows the ordinary authenticated read policy; any branch that creates source work requires acquire scope and otherwise fails with zero durable/provider work. Neither non-effective branch may contact a provider, create an acquisition operation, publish another blob, or recreate deleted predecessor bytes. Test exact-id hit, complete exact-id no-match, cross-source/same-period non-substitution, incomplete provider scope, network-disabled exact metadata miss, retained non-effective pinned observations, never-downloaded non-effective observations, and physically deleted predecessors separately, plus a specified inactive/delisted instrument using bounded local-first lookup/acquisition without changing bootstrap/daily denominator, cursor scope, completion rate, or triggering a historical delisted-universe scan.
- [x] 7.3 Implement globally single-flight asynchronous acquisition operations keyed by a versioned acquisition work fingerprint over operation type, normalized scope, acquisition/retention policies, provider route/capability matrix, classifier/integrity policy, relevant configuration, and accepted work/network/storage bounds, plus principal-scoped subscriptions/opaque handles with separate status/stage/outcome/disposition enums, authorization/rate bounds, polling projections, cancellation/expiry policy that cannot cancel other subscribers' work, restart recovery, actionable diagnostics, and versioned idempotency retention/tombstone behavior where internal operation status never uses `expired`; only caller asset/consumer projections may expire, replay within retention returns the original handle, and an expired key cannot silently create new work. Prove normalized equivalent inputs reuse one operation while route, limit, integrity, or policy changes do not reuse incompatible active work.
- [x] 7.4 Deliver durable monotonic effective-asset outbox events with per-consumer checkpoints and idempotent replay. Events SHALL always record the trigger origin and dispatch-policy version; only an explicit consumer continuation or scheduler dependency policy may enqueue parser work for `added|repaired`, while `replaced|withdrawn|deleted` events must invalidate only lineage whose normalized selector/cutoff evidence scope is changed and may enqueue reprocessing only under the declared consumer policy. Prove default-effective results become stale on effective change while exact-observation/knowledge-cutoff results remain current for out-of-scope later events. Generic ensure SHALL create no consumer operation.
- [x] 7.5 Support exact-filing attachment/hash/observation pins and version 1 detach-only `asset_request_id` cancellation, including last-subscriber behavior, repeat DELETE 200, and no cancellation propagation into internal acquisition, linked `consumer_request_id`, or already-started consumer processing; pending continuation cancellation remains the consumer-request DELETE contract. Keep operator batch stop and consumer-domain cooperative stop as separate contracts and add a cross-spec consistency test.

## 8. Business Consumer Migration

- [x] 8.1 Change broker annual-report acquisition to request shared assets while preserving listed-broker scope, embedded-table parser, supplementary report path, financial facts, and parser manifests; keep semiannual use behind its explicit V1 gate, classify standalone risk-control indicator reports as supplementary/validation only, and preserve dual lineage plus configured DCF source priority.
- [x] 8.2 Prove broker parsing produces equivalent facts and performs zero provider requests and zero duplicate archive writes when a shared asset is present; use one parser/storage/source-priority rule for backfill and daily event processing, and prove a financial-disclosure event without a qualifying annual-report asset event cannot trigger formal annual-report acquisition.
- [x] 8.3 Change business-profile acquisition and exact-filing reuse to request shared assets while preserving page artifacts, sections, extracted text/text hashes, semantic/LLM results, review evidence, promotion/facts, parser identity, and knowledge-cutoff behavior, including explicit metadata-only historical results and zero redownload for any non-effective observation; prove a same-filing/same-observation/same-hash path change leaves processing identity and equivalent outputs unchanged.
- [x] 8.4 Remove formal annual-report discovery/download/final-root ownership from `BusinessProfileDocumentArchiveService`, convert `AnnualReportAssetCatalog` and existing DataManager methods into read-through compatibility adapters, then disable legacy business-owned annual-report writes only after dual-read identity/hash reconciliation.
- [x] 8.5 Link all affected business outputs to shared asset id, source filing, report period, content hash, effective-correction state, selector/cutoff-resolved observation, independent consumer request lifecycle, and result `current|stale|reprocessing` state without coupling shared validity to parser status; prevent stale broker facts from being exposed or consumed by DCF as current while preserving historically correct pinned/cutoff results after later observations; prove that an available official JSON/XBRL/canonical numeric-fact source takes precedence for structured valuation reads without annual-report ensure, PDF parsing, or provider activity; persist consumer-specific continuations and the immediately-created `consumer_request_id` so a front-facing business command queues/reuses one consumer operation on local hit or after asset readiness, exposes terminal failure/blocking separately, and never starts unrelated consumers from generic ensure.
- [x] 8.6 Implement the business-profile and broker risk-control protected POST command adapters (or register exact equivalent existing routes in the OpenAPI snapshot) with a versioned `BusinessAnnualReportProcessRequest` that reuses generic ensure's selector/identity, `allow_network`, integrity, bounded wait, cutoff, and exact-observation pin rules; domain processing permission plus separate acquire permission; server-computed canonical processing fingerprint from a registered parser/profile/configuration with unknown-profile 422 and expected-fingerprint mismatch 409; caller idempotency/conflict handling; immediate `consumer_request_id`; `pending_asset` transition; current-result identity bound to the selector/cutoff-resolved observation and asset id/hash; completed/current local hit -> HTTP 200; parser-pending or stale/reprocessing local hit -> HTTP 202; asset-miss -> HTTP 202 with both handles; persisted terminal-missing handle/Location without Retry-After; deterministic blocked/forbidden/conflict responses; consumer-request `Location`; linked asset-request URL; and zero duplicate consumer operations.

## 9. DataManager And AI/API Client Integration

- [x] 9.1 Add DataManager list, get, versioned-policy ensure, redacted readiness, caller-scoped asset-request status, consumer-continuation status, and controlled-stream methods backed only by the shared asset service; list filters SHALL include instrument, fiscal year, source, canonical `source_announcement_id`/compatible `filing_id`, integrity, acquisition status, effective state, and local availability with deterministic pagination and explicit inclusion of current-effective, metadata-only, superseded, withdrawn, and historical records; expose canonical source projection plus stable equivalent-source-filing set/policy/evidence hash, and make controlled streams acquire/release a read lease and revalidate asset state, mount identity, length, and hash without accepting caller paths. Public asset-id streams return 410 for superseded/deleted assets; only an authorized internal DataManager/service exact-observation pin may stream a retained non-effective byte through a non-public controlled handle.
- [x] 9.2 Fix additive FastAPI/OpenAPI contracts for the exact list/effective/ensure, asset-request status and DELETE, consumer-request status and DELETE, readiness, asset-id content, and one protected business-profile plus one protected broker annual-report processing command; freeze the existing company-profile GET as `/api/v1/research/company/{instrument_id}/business-profile`, and declare broker V1 API-client status to be the protected command plus consumer-request projection unless a separate broker fact GET is explicitly registered/snapshotted. Bind each business command/read surface to its consumer owner, enforce zero-network GET, single-scope ensure, principal-scoped opaque handles without internal operation ids, deterministic route precedence, versioned response/error schemas, and HTTP 200/202/401/403/404/409/410/422/429/503 mappings. Asynchronous business-command 202 responses SHALL include `Location` and `Retry-After`; terminal missing 200 responses SHALL omit `Retry-After`, while authentication/cross-owner non-disclosure 404 responses are a separate policy and SHALL not create work.
- [x] 9.3 Implement a real trusted identity/permission boundary for acquire, content, request-subscription ownership, operator readiness, business-profile processing, and broker risk-control processing; compose domain and acquire scopes so a local business action cannot authorize network acquisition indirectly, and when the boundary is not configured make registered protected endpoints fail before lookup/validation with HTTP 503 `authorization_boundary_unavailable` and zero operation/provider activity.
- [x] 9.4 Separate the versioned API-facing `asset_availability=local_valid|metadata_only|missing|ambiguous|corrupt|superseded|blocked` vocabulary from per-instrument `bootstrap_asset_status=available|confirmed_missing|retryable|blocked`, exact-content availability, provisional/final/no-winner effective-decision state, ensure disposition, caller `asset_request_status=active|cancelled|expired`, internal operation status, operation stage, expected-period coverage, batch outcome, result origin, consumer-request lifecycle, and consumer-result `unavailable|current|stale|reprocessing` state; DELETE/expiry SHALL change only the request projection, and a non-applicable operation stage SHALL use the versioned null/not-applicable value rather than fabricated `completed`. Expose schema-defined optional/nullable shared lineage in the company-profile GET and symmetrically through the broker command/consumer-request projection (or an explicitly registered broker fact GET), including canonical/equivalent source evidence, without local paths or stale broker facts entering current DCF inputs.
- [x] 9.5 Integrate status, explicit acquire, idempotent caller-owned polling, safe content access, and correction-stale behavior into the AI/API-only caller contracts; preserve the existing business-profile GET while registering optional/nullable `source_assets.annual_report_asset` and `consumer_processing_status` properties with explicit OpenAPI/fixture mappings; expose `content_url` only for `asset_availability=local_valid` and `null` for metadata-only, missing, ambiguous, corrupt, superseded, blocked, or retained-internal-only content; bind API-client acceptance to the frozen backend candidate and OpenAPI contract version, with reproducible evidence for authorized explicit acquisition, authorization denial before work, active duplicate-submit suppression, `Location`/`Retry-After` polling through the caller-owned request resource, and safe content streaming. This project has no external Web UI dependency; API-client acceptance SHALL remain separate from production rollout and consumer-cutover gates.
- [x] 9.6 Add OpenAPI snapshot and API/consumer tests proving every GET is zero-network; asset-availability, exact-content, effective-decision, operation, disposition, expected-period, and consumer states round-trip without enum mixing; list filters/pagination return the specified effective and historical projections without implicit ensure; aggregate instrument GET state leaks no other principal's operation; selector forms and ensure policy fields are mutually exclusive/complete/identity-consistent/path-free; canonical `source_announcement_id` and compatibility `filing_id` normalize deterministically and conflicting dual input returns 422; a non-effective exact filing returns HTTP 200 `local_miss` plus `superseded|metadata_only` according to prior effectiveness and exact-content state with no request handle/provider work/blob, while the internal DataManager exact-observation handle may read a retained non-effective byte and the public asset-id stream returns 410; current content streams return safe `Content-Disposition` filename, `Content-Type=application/pdf`, and verified `Content-Length`, while superseded/deleted 410 takes precedence over current-corrupt 409; same-key/same-fingerprint POST reuses one asset or consumer subscription through terminal retention while same-key/different-fingerprint returns 409, and after expiry both request types retain the original opaque handle with `expired` projection while requiring a new key for new work; two principals share one underlying acquisition but receive isolated handles/continuations and cancelling one leaves the other's work active without cancelling its linked consumer request; pending/stale/acquiring business commands return 202 with `Retry-After`, terminal missing returns 200 without it, local-miss/network-disabled with domain scope returns 200 without asset work, missing network-enabled without acquire scope returns 403/404 without work, business-command `Location` always targets the consumer request while the body links any asset request; pre-work infrastructure blockers return POST 503 with no work while post-acceptance blockers remain polling HTTP 200; fixed route precedence and response schemas hold; authorization-unavailable precedes protected lookup/validation while redacted readiness remains read-only; restart recovery and owner isolation work; responses leak no internal operation id or path; public streams reject superseded/deleted/corrupt assets; later observations do not invalidate correct pinned/cutoff historical results; and legacy fields remain compatible.
- [x] 9.7 Add caller-scoped `consumer_request_id` DataManager/FastAPI/OpenAPI status and logical cancellation resources with owner/operator authorization, linked `asset_request_id`, independent request-lifecycle/result-freshness projection from `pending_asset` through processing/result/error/`expired`, common non-disclosure behavior, versioned retention/tombstones, durable post-cancel/expiry GET, repeat-DELETE HTTP 200 only for cancelled requests, accepted cooperative-stop HTTP 202, terminal `completed|missing|failed|expired`/current-result and non-stoppable started-blocked HTTP 409 `request_not_cancellable`, cancellable not-started blocked HTTP 200 with preserved blocker evidence, unknown/cross-owner 404, and independent asset-versus-business polling tests. Consumer-request DELETE SHALL NOT mutate the linked asset request or internal acquisition; asset detachment remains a separate DELETE.
- [x] 9.8 Add API/consumer integration tests proving the business command adapters create the consumer request before acquisition completes, preserve the same handle through asset readiness, return 200 only when request lifecycle is completed and result freshness is current for the selector/cutoff-resolved observation plus processing fingerprint, return 202 for pending/stale/reprocessing parser work or asset acquisition, persist a queryable terminal-missing handle/Location without Retry-After, enforce combined domain/acquire authorization, preserve explicit provisional/pending-correction behavior without new consumer enqueue for an unverified same-batch correction, prevent stale broker facts from current API/DCF use, preserve correct pinned/cutoff historical results after later corrections, and deterministically return terminal missing 200, pre-work infrastructure blocked 503, post-acceptance blocked status via polling 200, forbidden 403/404, or ambiguity/conflict 409 without unintended provider activity.

## 10. Backup Observability And Operations

- [x] 10.1 Implement incremental content-addressed archive backup to a verified independent storage failure domain by enumerating catalog-required blobs, including promoted adopted files still at controlled legacy paths, every correction-predecessor, withdrawal-tombstone, or legacy-duplicate blob in the immutable version 1 recovery manifest, and every predecessor held by a `planned|deleting` deletion intent/recovery pin before its manifest row exists; use missing-only copy, mandatory revalidation of existing hash-named targets, ordinary-run preservation of mismatched target path/bytes/mtime, operator-authorized quarantine/repair without watermark advance, atomic temporary-file publication, size/hash validation, mount-source checks, target-side warning/hard-reserve/planned-byte gates, crash recovery before/after publication, manifest commit, catalog snapshot, and watermark commit, a file manifest watermark paired with a recoverable catalog database snapshot, freshness state, unprotected bytes, and explicit mount/capacity failure handling. Persist post-snapshot catalog/outbox/operation/lineage/audit increments required by the declared RPO in an append-only independent-failure-domain recovery journal with ordered ids, integrity hashes, predecessor/coverage watermarks, and truncation detection; version 1 performs no automatic backup-blob GC.
- [x] 10.2 Add persisted readiness and operational reports for active-universe coverage, discovery gaps, attachment readiness, integrity, retries, storage, stale `.part`/quarantine bytes and age, unprotected bytes, backup freshness, scheduler state, and per-consumer migration; calculate asset-scheduler readiness independently from consumer cutover, expose independent `legacy_write_stop_allowed` and `duplicate_cleanup_allowed` gates, and include `missing_attachment_count`, `estimated_required_bytes`, estimate basis, estimate `as_of`, configuration fingerprint, and an explicit unavailable/indeterminate estimate state, while keeping known lengths, unknown-length reservations, temporary publication overhead, and old-plus-new replacement peak bytes separate. Include recent-run retention, last successful cutoff, stale heartbeat, consecutive failures, cursor lag, oldest backlog age, alert thresholds, and separate redacted API-caller versus operator diagnostics. Add configurable predecessor-cleanup warning/hard ages to production config, template, schema parity, versioned fingerprint, and audit; reject non-positive values or `warning_age >= hard_age`. An unresolved distinct `planned|deleting|failed` predecessor degrades readiness at warning age, blocks unique-storage completion and enters operator repair at hard age, and reports oldest age/state/error without rolling back the effective replacement or blocking unrelated verified reads. Threshold crossing SHALL NOT release a retention pin, change deletion-intent required-set enumeration, or authorize unlink. Persist and reload complete daily/bootstrap report schemas, including daily exclusions/ambiguities, adoption/failure, effective additions/dereferences, repair cohorts, stage timings, universe snapshot identity, winner/duplicate counts, window completion, resume identity, bytes, free space, and unprotected bytes.
- [x] 10.3 Add `annual_report_asset_integrity_audit` and `annual_report_asset_backup` jobs plus repair commands that default to read-only and require a separately explicit bounded authorization/flag for each network repair, quarantine, link, move, or delete action; prove read-only audit changes no DB/file hash/mtime/watermark and that missing scope, unavailable identity boundary, missing action flag, or over-broad target causes zero operation, network, file, and watermark mutation.
- [x] 10.4 Implement and document paired application-version/database/file backup restore ordering, two-phase recovery-pair closure, and recovery-manifest reconstruction; distinguish isolated consumer rollback from true data-loss restore, require a write freeze, declared snapshot RPO, and ordered integrity-checked recovery-journal replay or proof from a persisted write-freeze watermark that no post-snapshot outbox/operation/lineage/audit increment exists before any overwrite. Inject a missing middle increment, out-of-order increment, tail truncation, and payload/hash tampering; every case SHALL block restore reopen. A migrated legacy catalog watermark/hint SHALL NOT synthesize recovery-pair closure without fresh bidirectional verification. Require full presence/length/hash reconciliation of every current-effective, retention-pinned, pending-deletion replacement/predecessor, every immutable indefinitely-active version 1 correction-predecessor or legacy-duplicate recovery blob, and every single-predecessor `withdrawal_tombstone` required blob, plus current-effective/no-winner projection, immutable decision-history, deletion-intent/outbox and consumer-lineage/current-result consistency; keep recovery-only predecessors/duplicates in the backup required set or isolated non-consumer-visible restore area rather than republishing them as second primary canonical attachments. Rebuild and compare derived retention-pin/readiness projections before re-enabling reads, writes, consumer startup, or predecessor deletion, while keeping sampled restore drills as additional routine evidence and performing no manifest retirement/GC. A restored multi-revision or withdrawal lineage SHALL contain no dangling, missing, overwritten, or contradictory decision edge.

## 11. Validation Rollout And Cleanup

- [x] 11.1 Run all repository, schema-field/migration, classifier, lifecycle, backfill, daily, API, scheduler, storage, backup, and consumer unit tests on temporary databases and archive roots, including post-rename reopen/hash verification, managed-alias pin truth, preflight-to-mutation/finalization remount races (same source/export with changed filesystem identity, same identity becoming read-only, and post-unlink fallback-path absence), immutable multi-revision decision history, withdrawal with a fallback, withdrawal without replacement/no-winner tombstone and single-predecessor recovery, discovery stale-worker fencing, acquisition work-fingerprint compatibility, blocked-versus-failed retry mapping, consumer-failure isolation from asset state, and predecessor cleanup warning/hard-age projections. Finish with a full catalog/filesystem invariant audit proving every production-available `(instrument, fiscal_year)` has exactly one current winner, unavailable or withdrawn-without-replacement periods have none, aliases/backup/quarantine/candidate/withdrawn objects are never a second consumer-visible canonical asset, shared identical bytes retain only valid references, same-hash legal replacement produces no physical-unlink intent or false deletion audit, and any distinct superseded or withdrawn primary byte still under a deletion-blocking pin or pending/failed intent keeps the unique-storage completion gate unpassed until convergence; reject orphan current blobs and second same-period canonical attachments.
- [x] 11.2 Run bounded live provider probes for SSE, SZSE, and BSE originals, complete corrections, excluded summaries, success-empty windows, and managed-attachment version signals, recording classification, pagination, cursor advancement, overlap idempotency, conditional/version-signal support, bounded silent-byte verification feasibility, attachment, rate-limit, and publication-delay evidence. Include primary-failure probes that assert primary cursor/gap, fallback source-qualified cursor/`covered_until`, independent route-level required-coverage projection, and full-market readiness separately: a complete fallback advances only its own cursor, never the failed primary cursor; only an audited query-equivalent fallback result may satisfy the route-level projection and corresponding readiness, while a non-equivalent fallback preserves that route-level gap. Calibrate and persist the default three-calendar-day overlap (with config/policy fingerprint) with zero unbounded production writes, and record an explicit readiness limitation for any route that cannot prove silent-update detection.
- [x] 11.3 Run production archive inventory and shadow adoption, then reconcile database rows, file hashes, active-universe coverage, and duplicate bytes; persist a versioned timestamped capacity artifact containing primary/backup mount and failure-domain identities, total/used/free bytes, universe count, P95/P99/max, estimation assumptions, primary/backup required-set actual bytes, permanently retained recovery-manifest bytes, expected annual growth, temporary and old-plus-new peak bytes, planning horizon/headroom, backup-target capacity, configuration fingerprint, and freshness. Use that measured distribution to calibrate the annual-report attachment limit and unknown-length reservation budget with explicit approver; expired/config-mismatched artifacts SHALL fail the rollout gate, which validates every field before any production archive download or deletion.
- [ ] 11.4 Enable shared reads for broker and business-profile behind migration gates, process affected assets, and require zero duplicate network/archive activity plus compatible business outputs; verify the broker semiannual gate, supplementary standalone risk-control lineage and configured DCF priority, shared backfill/daily parser/storage/source policy, and no formal annual-report acquisition from a financial-disclosure event without a qualifying annual-report asset event; verify structured JSON/XBRL/canonical numeric-fact reads remain higher-priority and create no PDF/provider/parser work.
- [ ] 11.5 Verify canonical archive backup and restore, including a required blob outside a sampling cohort that must still block enablement; verify independent failure-domain identity, backup job operability, initial required-blob protection, paired watermark freshness, immutable multi-revision decision-history reconstruction, recovery-journal middle-gap/order/tail-truncation/payload-hash corruption rejection, legacy-hint non-closure migration, and the runtime backup-failure policy for metadata discovery, local reads, deletion, bounded attachment writes, restart-persistent unprotected byte/age accumulation, and verified-backup reset/unblock. Complete the latest-only bootstrap, rerun the full exactly-one-current-winner and unique-storage catalog/filesystem invariant audit with no unresolved convergence blocker, and prove daily cron enablement fails closed until configured coverage, integrity, storage, backup, asset-adoption, and handoff gates pass, then starts without a discovery gap even when business-profile and broker consumer migration gates remain disabled.
- [ ] 11.6 Disable each legacy annual-report writer and remove its redundant files/code only after that consumer's deletion plan is reviewed, every non-recovery/deletion-blocking pin is released, recovery protection has handed off to a durable manifest/required-set hold, backups are verified, and a temporary-root drill reconstructs legacy paths from the recovery manifest. Prove a single-consumer rollback changes only that consumer's gate and leaves independent daily maintenance/other consumers running; stop daily only for a separately audited shared-module safety blocker. After any physical cleanup, run an isolated post-cleanup consumer-rollback drill against the compatible application/catalog/file set without overwriting the live catalog, validate legacy consumer reads before startup/publication, and prove no shared metadata, lineage, recovery, operation, or audit rows are deleted; separately drill true data-loss paired restore with write freeze, RPO declaration, and post-snapshot increment replay/closure.
- [ ] 11.7 Complete the 1.8 bidirectional requirement-leaf/normative-clause registry without renumbering or dropping registered ids, then produce `evidence/release_trace_report.json` with exactly one evidence row per registered requirement leaf and normative clause, linking reproducible test node/command, bounded live-probe, operator, or API-client evidence path, accountable owner, final status, OpenSpec CLI/schema version, and retained strict-validation result; persist the API-client acceptance registry with `client_mode=ai_api_only`, frozen backend candidate, bound OpenAPI version, exact acquisition/authorization/idempotency/polling/content test evidence, and passed gate status. Revalidate registry schema, prior-baseline alias history, unique ids, exact bidirectional requirement/spec/task coverage, independently addressable SHALL clauses, and no orphan rows in release acceptance; record explicit product/operations sign-off that V1 deletion means primary-archive unlink with governed recovery-byte retention and no normal point-in-time replay or all-media secure erasure, plus explicit product sign-off that V1 “full market” is the independently reconciled snapshot-time still-listed SSE/SZSE/BSE RMB A-share universe and excludes historical delisted securities from scheduled coverage. Fail acceptance when any requirements leaf or normative clause lacks its pre-registered stable id, exact task, evidence owner, or reproducible evidence; any checked task lacks exact evidence; strict validation fails; either scope/retention sign-off is absent; or the API-client gate is not passed. No external Web UI repository, owner, page, or deployment is required, and a passed API-client gate SHALL NOT bypass the production rollout or consumer-cutover gates.
