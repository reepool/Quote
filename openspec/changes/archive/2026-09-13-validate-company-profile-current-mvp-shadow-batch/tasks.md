## 1. Freeze the current-MVP validation contract

- [x] 1.1 Bind the existing twenty-report manifest, PDF hashes, ownership-aware v6
  Evidence plan/preparation, September 11 reviewed baseline, current implementation,
  logical route, dynamic token policy, 300-second deadline, 600-call ceiling, readiness
  gates, new batch ID, absent output path, and research-only boundary.
- [x] 1.2 Record a credential-free description and fingerprint for exactly four eligible
  `semantic_extraction` pool members, common `json_object` support, and the contracted
  failover depth without modifying or staging the task-start LLM configuration.

## 2. Implement the narrow replay mode

- [x] 2.1 Add `current-mvp-semantic-replay` to the existing shadow operator and delegate
  report execution to `ManufacturingMaterialsShadowBatchService`; do not add a second
  provider or semantic loop.
- [x] 2.2 Fix the stale `_provider_for_scope` call so historical frozen modes explicitly
  use fixed budgets and the new current-MVP mode explicitly enables existing dynamic
  scope-size token budgets.
- [x] 2.3 Reject manifest/PDF/plan/preparation/implementation/baseline/route/pool/budget/
  batch-ID drift, obsolete Gemini-only identities, and output reuse before the first
  semantic provider request.
- [x] 2.4 Add focused provider-free regressions for exact admission, four-member pool
  eligibility, structured output, failover depth, dynamic-budget forwarding, mode
  isolation, output immutability, and unchanged production restrictions.

## 3. Verify admission before provider use

- [x] 3.1 Run focused tests, Ruff, strict OpenSpec validation, bound-file hash checks,
  and output-absence checks without making an LLM request.
- [x] 3.2 Perform one read-only connectivity preflight for the configured Scorpio, ZAI,
  and DeepSeek endpoints, using the authorized external path if sandbox DNS or network
  policy interferes, and persist a credential-free typed receipt.

## 4. Execute the sole current-MVP batch

- [x] 4.1 Start exactly one provider-bearing twenty-report run under
  `manufacturing-materials-shadow-current-mvp-four-pool-20260912-a`; after the first
  semantic request, do not tune prompts, fields, routes, models, token thresholds, or
  deadlines and do not target-rerun, splice, or create a replacement identity.
- [x] 4.2 Preserve all report-local successes, holds, and typed failures under the
  existing isolation and verify that historical batches and production consumers remain
  unchanged.

## 5. Source-review and close the validation

- [x] 5.1 Build the source-bound review package covering every blocker, caveat, and
  unresolved item plus one stable accepted or legal-empty result per core chapter, then
  record reviewed outcomes without changing extraction semantics.
- [x] 5.2 Produce the empirical readiness audit and immutable comparison against the
  September 11 baseline using execution >=95%, usable-report rate >=90%, Evidence
  traceability 100%, complete review, sampled precision >=99%, zero critical semantic
  errors, and unresolved-review median/p90 <=2/5.
- [x] 5.3 Close as `ready`, `hold`, or `failed` from observed results; retain
  `production_authorization=not_authorized`, Stage 6 closure, and approved/backfill,
  CommodityExposure, ValueChainRole, and DCF restrictions.
- [x] 5.4 Run targeted regressions, Ruff, artifact/hash checks, `git diff --check`, and
  strict OpenSpec validation; review only blocking defects, then commit and push only
  this isolated change while preserving all task-start workspace changes.
