## Why

The precision-closure replay failed before producing a report because sandboxed Python/httpx requests could not resolve Scorpio even though a curl preflight succeeded. The user has explicitly authorized external-sandbox network execution for this recurring condition, so one new-ID replay must run from that path while every business and model input remains frozen.

## What Changes

- Admit one new immutable external-network replay identity bound to the same twenty-report cohort, PDFs, corrected v3 Evidence, stability evidence, precision-closure audit, Gemini profile, budgets, source-review rule, and readiness gates.
- Require the provider-bearing process itself—not merely curl—to start through the authorized external-sandbox path; do not reuse the failed sandbox batch identity or its empty directory.
- Run the full cohort once through the existing report-local service, then source-review and compare it against `manufacturing-materials-shadow-stability-gemini-20260909-a`.
- Close as `ready`, `hold`, or `failed` without targeted reruns, tuning, splicing, or production promotion.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `company-profile-manufacturing-materials-shadow-batch`: authorize one new-ID external-network execution after the documented sandbox DNS failure while preserving all frozen empirical contracts.

## Impact

- Affected implementation: one narrow operator contract/mode and provider-free admission tests.
- Affected operation: the provider-bearing command is launched with the existing user-authorized external-sandbox permission path.
- No Evidence, semantic rules, model, tokens, deadlines, call ceiling, public API, database, Gold, production writer, scheduler/backfill, downstream publication, or Stage 6 change.
