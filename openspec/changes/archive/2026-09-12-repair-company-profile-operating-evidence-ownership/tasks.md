## 1. Freeze the causal comparison

- [x] 1.1 Bind the frozen twenty-report manifest, Evidence plan, authoritative replay,
  and the seventeen failed operating scopes by identity/hash for a provider-free audit.
- [x] 1.2 Record the current field sets and page-by-field bindings, including known
  positive capacity and production/sales/inventory owners that must be retained.

## 2. Correct operating field inference

- [x] 2.1 Derive operating scope fields from field-specific text on direct candidate
  pages only; prevent adjacent context, section keys, selector reasons, headings, and
  anchor terms from adding numeric fields.
- [x] 2.2 Add focused regressions for false adjacent capacity/inventory expansion and
  positive current capacity, under-construction capacity, and production/sales/inventory
  tables.

## 3. Preserve and consume Evidence ownership

- [x] 3.1 Bind direct operating Evidence to the fields supported by its own page and keep
  non-owning bounded context Evidence unbound.
- [x] 3.2 Make the Stage 5 request builder respect explicit bindings, filter inactive
  bindings, retain unbound context, and use the historical cross-product only when all
  Evidence is unbound.
- [x] 3.3 Add focused preparer/service tests for exact bindings, context-only Evidence,
  mixed explicit/unbound scopes, inactive fields, and the legacy compatibility path.

## 4. Produce provider-free proof

- [x] 4.1 Rebuild the frozen twenty-report plan/preparation without a provider and write
  a hash-bound comparison audit covering all seventeen failed operating scopes.
- [x] 4.2 Confirm unsupported field/binding removal, positive disclosure retention,
  `provider_calls=0`, immutable historical artifacts, unchanged token budgets, and
  `production_authorization=not_authorized`.

## 5. Verify and close

- [x] 5.1 Run focused company-profile tests, Ruff, strict OpenSpec validation, audit/hash
  checks, and `git diff --check`; review and fix only blocking defects.
- [x] 5.2 Commit and push only the isolated change files without touching pre-existing
  workspace changes, then pause and summarize current development/research status and
  the bounded dynamic-token follow-up recommendation.
