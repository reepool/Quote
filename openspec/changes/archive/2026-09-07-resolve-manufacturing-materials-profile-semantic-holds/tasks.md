## 1. Baseline and adjudication contract

- [x] 1.1 Record run-f as the immutable problem baseline, enumerate every incomplete request scope and subject blocker for the four reports, and confirm all later runs require new run IDs.
- [x] 1.2 Add a versioned adjudication record format for sample/scope/runtime target, Evidence, blocker, decision, contract basis, reviewer, and research-only status.
- [x] 1.3 Create the initial adjudication ledger covering subject scope, Ningde business-change coverage, Putailai processing volume, and AVIC Chengdu same-control columns without copying Gold values into runtime results.
- [x] 1.4 Update the current stage-five audit/index to identify this change as the only next company-profile work and retain `production_authorization=not_authorized`.

## 2. Minimal semantic corrections

- [x] 2.1 Add tests proving a complete composite source-native processing label can verify as one `processing_volume` without creating or requiring same-anchor `sales_volume`.
- [x] 2.2 Add tests proving explicit no-major-business-change text produces evidenced `not_applicable` coverage separate from a consolidation-scope event.
- [x] 2.3 Add tests proving consolidated subject requires direct wording or documented same-report numeric reconciliation and that business-segment measured objects do not fabricate report subject.
- [x] 2.4 Add tests proving same-control adjusted/pre-adjustment columns retain independent comparison basis, subject evidence, periods, and knowledge time without overwrite.
- [x] 2.5 Implement only the provider/workflow/coverage changes required by 2.1-2.4, keeping extract, at most one repair, independent verify, and the existing model enums unchanged.

## 3. Real committed-output benchmark

- [x] 3.1 Extend negative-case benchmark results with evaluation reason and inspected runtime target identities while preserving strict immutable models.
- [x] 3.2 Replace caller-supplied negative-case pass booleans with fixed manufacturing/materials v1 evaluators over committed records, dispositions, coverage, Evidence, and research projection.
- [x] 3.3 Implement direct runtime evaluation for all 19 frozen negative cases; a missing trigger or zero accepted output must remain unevaluated or failed rather than pass.
- [x] 3.4 Add tests using real-shaped committed bundles for prohibited outputs, legal empty results, missing triggers, and exact 24-Gold/19-negative coverage.
- [x] 3.5 Add a thin local post-run benchmark command that reads one committed isolated run and writes `post-run-benchmark.json` atomically without accessing production state.

## 4. Offline acceptance and targeted reruns

- [x] 4.1 Run company-profile stage-four/stage-five unit tests and Ruff for the changed files; fix only blockers affecting the four-report contract.
- [x] 4.2 Validate the OpenSpec change strictly before any live provider call.
- [x] 4.3 Run new targeted semantic runs for the affected scopes under new run IDs using the approved Evidence plan and common LLM gateway.
- [x] 4.4 Record targeted results in the adjudication ledger and keep any unsupported subject, incomplete coverage, or verifier conflict as `hold`.

## 5. New authoritative four-report result

- [x] 5.1 If all targeted corrections behave according to the frozen contract, run one complete four-report semantic slice under a new run ID without mixing run-f or targeted records.
- [x] 5.2 Execute the automatic post-run Gold and negative benchmark against the new committed run and retain every failed or unevaluated item.
- [x] 5.3 Generate one Chinese researcher-readable profile per report plus a cross-report adjudication summary distinguishing facts, legal empty states, unresolved items, and production non-authorization.
- [x] 5.4 Perform the final isolated-output garbage audit and list all retained authoritative, hold, targeted, failed, and diagnostic bundles without deleting review evidence.

## 6. Acceptance and handoff

- [x] 6.1 Verify each of the four reports has an explicit pass/hold decision, no average score hides a blocker, and no research acceptance became production approval.
- [x] 6.2 Retain the exact report/slice status under the approved research acceptance policy; do not require Gold string equality or untriggered real-report negatives, and do not register usability while a required execution or frozen semantic blocker remains.
- [x] 6.3 Verify old backfill, scheduler, API, Telegram, approved tables, CommodityExposure, ValueChainRole, DCF, and stage-six reset were not invoked or modified.
- [x] 6.4 Run targeted regression tests, strict OpenSpec validation, review the isolated diff, and prepare the change for user acceptance and later archive.
