# 阶段 4 独立 dossier

这三份 dossier 只服务 `scope-manufacturing-materials-stage4-minimum-slice` 的任务 2.1。每份只记录该报告自己的正文证据。

- 不在这里决定哪个章节是最小竖切。
- 不把只在一份报告里出现的字段标成 required。
- 不使用 Gold 或行业常识补运行时事实。
- `302132.SZ` 不是本卡定义样本。
- `600004.SH` / `600006.SH` 不是本卡定义样本。

物理页码是本地 PDF 的 1-based 页序，与 pypdf 页数一致。阅读范围是含相关标题的正文页，不是 Gold。
