## 1. Closed review contract

- [x] 1.1 Add typed Activity-only review request/result contracts bound to the immutable source run and report hashes.
- [x] 1.2 Freeze the three approved `000717.SZ` decisions with exact review IDs, runtime targets, Evidence, source actors, source wording, and `subject_scope=unclear`.

## 2. Offline adjudication path

- [x] 2.1 Add one Stage 5 service operation that validates and applies only `explicit_economic_relationship` to `direct_grammatical_actor`, closes derived Activity coverage, and recomputes the existing research projection, benchmark, and report state.
- [x] 2.2 Add a thin offline operator that writes atomically outside the immutable input run and performs no provider or PDF work.

## 3. Validation result

- [x] 3.1 Add focused positive and fail-closed tests for hash, Evidence, actor, target, prior-basis, and subject-scope boundaries.
- [x] 3.2 Apply the three decisions once to `stage55-second-oos-completion-000717-20260909-c`, preserve source hashes, and record the derived status and research-only audit.

## 4. Closure

- [x] 4.1 Run focused tests, Ruff, strict OpenSpec validation, review blocking defects, sync the delta spec, archive the change, and submit only this task’s isolated files.
