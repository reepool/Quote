## 1. Freeze transport-recovery admission

- [x] 1.1 Build a provider-free recovery proof binding the archived failed replay,
  twenty-report manifest/PDFs, v6 plan/preparation, ownership audits, current
  implementation, Gemini route, fixed budgets, review gates, new batch ID, and absent
  output.
- [x] 1.2 Verify all frozen hashes and record that the only changed experimental variable
  is the authorized external execution path.

## 2. Add the narrow external replay mode

- [x] 2.1 Add one external ownership-recovery contract/mode to the existing shadow
  operator and delegate to the existing report-local service.
- [x] 2.2 Reject failed-attempt, cohort/PDF, plan/preparation, implementation, route,
  budget, batch-ID, or output drift before provider construction.
- [x] 2.3 Add focused provider-free tests for exact admission, mode isolation, drift
  rejection, output absence, fixed budgets, and unchanged production restrictions.

## 3. Execute the sole external replay

- [x] 3.1 Run focused tests, Ruff, strict OpenSpec validation, proof/hash checks, and
  output-absence checks before network use.
- [x] 3.2 Run one read-only Scorpio/Gemini preflight through the authorized external path
  and persist its typed receipt without generating candidates.
- [x] 3.3 Launch exactly one complete provider-bearing twenty-report replay through the
  same external path; preserve every report-local success or typed failure and do not
  tune, replace, target-rerun, or splice results.

## 4. Review and quantify

- [x] 4.1 Generate the frozen six-chapter source review package and record decisions for
  every blocker, caveat, unresolved item, and stable per-chapter sample.
- [x] 4.2 Generate empirical readiness, seventeen-scope ownership recurrence, and
  immutable before/after comparison artifacts with all required hashes and metrics.

## 5. Close and verify

- [x] 5.1 Record `ready`, `hold`, or `failed` from the frozen gates while retaining
  `production_authorization=not_authorized`, fixed token budgets, Stage 6 closure, and
  all production-consumer restrictions.
- [x] 5.2 Run targeted regressions, Ruff, artifact/hash checks, `git diff --check`, strict
  OpenSpec validation, and blocking-defect Review; then commit and push only this change
  without touching pre-existing workspace files.
