## Why

Provider-free fixtures prove the bounded segment-partition normalization rules, but the September 11, 2026 batch did not persist the raw conflicting partition values. A two-scope live validation is needed to determine whether the real Gemini responses now reconcile without committing another twenty-report replay.

## What Changes

- Bind the immutable September 11 shadow batch and exactly two previously failing segment scopes: one `reported_period` conflict and one incomplete `报告分部` heading case.
- Run a read-only Scorpio/Gemini preflight, then execute each frozen prepared scope once through the existing Stage 5 application service under a new validation identity and bounded provider-call budget.
- Persist source-bound extract/verify traces and classify each case as resolved, retained substantive failure, or execution failure without changing the historical batch.
- Keep the result non-authoritative for batch readiness and retain `production_authorization=not_authorized`; a full twenty-report replay remains a separate decision.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `company-profile-manufacturing-materials-shadow-batch`: require a bounded, new-identity live validation of the two normalization classes before deciding whether another full-cohort replay is justified.

## Impact

- One change-local validation contract, thin one-time operator, and empirical receipt.
- Existing `ManufacturingMaterialsProfileSliceService`, Gemini route, provider budgets, semantic schema, Evidence, historical batches, Gold, readiness thresholds, and production paths remain unchanged.
