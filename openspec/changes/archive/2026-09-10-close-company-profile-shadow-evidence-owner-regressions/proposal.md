## Why

The external twenty-report replay removed the sandbox DNS blocker and reduced critical semantic errors to zero. Immediate source recheck found that one of the initially marked eight noncritical rows (`920033.BJ`) is actually a valid explicit material-section legal empty; the audit baseline must be corrected, and the remaining six Evidence-owner failures plus one cross-reference overview failure must be closed on the actual planning path before another LLM replay.

## What Changes

- Bind operating-quantity/capacity legal-empty conclusions to quantity/capacity owners, while allowing an explicit issuer capacity disclosure in a governed overview passage to serve as positive capacity Evidence.
- Bind material legal-empty conclusions to procurement/material owners and prevent shareholder, research, EHS, or adjacent context from closing the field.
- Bind segment-dimension legal-empty conclusions to segment or revenue/cost owners rather than industry narrative.
- Require an accepted `business_overview_source` to contain substantive issuer business text instead of only a cross-reference.
- Publish a hash-bound additive correction for the `920033.BJ` review outcome without changing the archived v1 audit or source batch.
- Close the seven confirmed error rows provider-free through the existing planner/normalizer/service path and emit a hash-bound closure audit; do not run an LLM or reinterpret the immutable replay.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `company-profile-manufacturing-materials-shadow-batch`: first correct the source-review baseline, then close the seven confirmed owner/substance regressions provider-free before another cohort replay can be proposed.

## Impact

- Affected implementation: existing shadow Evidence planning/owner selection and bounded Stage 5 acceptance checks only.
