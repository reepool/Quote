# Ownership Replay Closeout

- Result: `failed`.
- The provider-free v6 ownership plan was admitted exactly and the synthetic Scorpio/Gemini preflight succeeded.
- The authoritative provider-bearing process then persisted one complete report with eight of eight scopes failing as `deadline_exceeded`; terminal transport diagnostics classified every completed request as `dns_failure` during `connect`, before any HTTP response or model output.
- The first persisted report is not one of the seventeen affected ownership scopes. Therefore this run provides no causal evidence about whether the v6 ownership correction improves semantic yield.
- An external read-only check resolved `scorpio.reepool.com` and reached its HTTPS endpoint immediately, while the formal process remained inside a restricted network namespace. The failure is classified as execution-path transport, not model quality, token budget, schema, or Evidence ownership.
- The process was stopped after the first complete report to avoid repeating the same 300-second pre-HTTP failure across the remaining cohort. No replacement identity, targeted rerun, candidate splice, historical mutation, or production write was performed.
- `production_authorization=not_authorized`; Stage 6, approved storage, scheduler/backfill, commodity exposure, value-chain publication, and DCF remain closed.

The next empirical ownership replay, if approved in a separate change, must launch the provider-bearing process through the authorized external network path from the start. It must retain the frozen v6 Evidence inputs and fixed provider parameters so transport recovery is not confused with semantic or token tuning.
