## 1. Freeze the post-repair replay contract

- [x] 1.1 Bind the new immutable batch ID to the frozen twenty-report manifest/PDF hashes, v5 Evidence plan and audits, routing/continuation baseline, archived segment closure audit, implementation hashes, Gemini profile, budgets, readiness gates, and production boundary.
- [x] 1.2 Add an admission receipt that verifies all bound inputs and proves the new output directory is absent before any provider request.

## 2. Implement narrow admission and execution mode

- [x] 2.1 Add the post-segment-repair replay mode to the existing shadow operator and delegate execution to the report-local service without adding a second semantic loop.
- [x] 2.2 Reject generic execution, changed plan/audit/implementation/profile/budget inputs, reused output identity, and any attempt to use historical or model-comparison candidates.
- [x] 2.3 Add focused provider-free tests for valid admission, every drift class, output reuse, mode isolation, and unchanged research-only production boundaries.

## 3. Execute the sole provider-bearing replay

- [x] 3.1 Run one read-only Scorpio/Gemini connectivity and profile preflight; if the sandbox reports likely DNS or network-permission failure, repeat only that read-only check through the authorized external path.
- [ ] 3.2 Run the frozen twenty-report cohort exactly once with the new batch ID and existing Gemini/output/deadline/call budgets, preserving every typed report-local success or failure without tuning, targeted reruns, model substitution, or result splicing.

## 4. Produce source-bound evaluation artifacts

- [ ] 4.1 Generate the complete source-review package and outcomes for all six core chapters and record segment-financial recurrence against the prior 30 blockers, 135 unresolved rows, and 25 local merge failures.
- [ ] 4.2 Generate the readiness audit and immutable before/after comparison binding both batches and all review, manifest, Evidence, implementation, and audit inputs by content hash.

## 5. Close the validation honestly

- [ ] 5.1 Record `ready`, `hold`, or `failed` from the frozen gates, list exact source-bound blockers, and retain `production_authorization=not_authorized` with every production path closed.
- [ ] 5.2 Run focused tests, Ruff, artifact/hash checks, `git diff --check`, and strict OpenSpec validation; review only blocking defects, then commit and push isolated change artifacts without touching pre-existing workspace files.
