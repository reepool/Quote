## 1. Freeze the failure contract

- [x] 1.1 Inventory all 29 frozen external-replay `candidate_schema_invalid` extract traces and classify the five observed failure families.
- [x] 1.2 Record bundle hashes, the absence of raw provider payloads, zero-provider replay boundaries, and unchanged production authorization.

## 2. Isolate invalid response items

- [x] 2.1 Add a backward-compatible typed rejected-item diagnostic to Stage 5 provider traces.
- [x] 2.2 Preserve valid candidate and coverage siblings while excluding item-local expansion and final-schema failures.
- [x] 2.3 Isolate ambiguous compact counterparty measurements and invalid business-regime legal-empty coverage without rewriting facts or Evidence.
- [x] 2.4 Keep envelope, request identity, and non-item call failures typed and fatal.

## 3. Prove provider-free behavior

- [x] 3.1 Add mixed valid/invalid regressions for context-only legal empty, contradictory business regime, control-only no-change, overview source mismatch, and ambiguous counterparty direction.
- [x] 3.2 Prove that an all-invalid required field remains unresolved and that a historical bundle without rejected-item diagnostics still validates.
- [x] 3.3 Generate an immutable provider-free audit that distinguishes equivalent fixture retention from unavailable historical salvage counts.

## 4. Validate the change

- [x] 4.1 Run focused provider/workflow/bundle tests and Ruff on changed Python files.
- [x] 4.2 Run strict OpenSpec validation, repository diff checks, and confirm the frozen external bundle hashes are unchanged.

## 5. Close the change

- [x] 5.1 Review the task diff for blocking defects and fix only current-scope issues.
- [x] 5.2 Sync the delta requirement to the main spec, archive the completed change, and commit/push only this task's isolated files.
- [x] 5.3 Publish the 5/5 checkpoint summary, including the separate trace-derived adaptive-token recommendation and remaining production gates.
