## Why

The first twenty-report shadow batch proved the end-to-end research path but failed the scale-readiness gates largely because each scope carried an over-broad chapter field set. The refined v2 Evidence plan removes that mechanical over-assignment provider-free; one controlled replay on the identical frozen cohort is now required to determine whether the smaller, source-aligned requests improve real semantic completion and review workload.

## What Changes

- Authorize exactly one new full twenty-report replay using the immutable shadow manifest, refined v2 Evidence plan, frozen Gemini logical profile, and the same output-token, timeout, and provider-call budgets as the v1 batch.
- Validate manifest, PDF-artifact, plan, preparation, provider profile, and output identity before any provider request; keep the v1 batch and all historical outputs immutable.
- Produce a deterministic before/after audit covering execution completion, report usability, accepted facts, Evidence traceability, frozen failure reasons, human-review median/p90, provider failures, tokens, and latency.
- Generate the existing source-bound review package for the new batch and disclose `ready`, `hold`, or `failed` without targeted reruns, cross-run splicing, semantic-policy changes, or rule relaxation.
- Keep all results research-only with `production_authorization=not_authorized`; the replay may establish a prerequisite for a later restricted promotion design but cannot open Stage 6 or any production writer.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `company-profile-manufacturing-materials-shadow-batch`: Add a one-run controlled replay contract and a hash-bound before/after empirical audit for the refined Evidence plan.

## Impact

- Reuses `ManufacturingMaterialsShadowBatchService`, `ShadowEvidencePreparer`, the existing shadow operator, review package, and readiness audit; only the minimum replay admission and comparison artifact logic is added.
- Reads the archived v1 manifest, v1 batch, v1 readiness audit, refined v2 plan, and provider-free refinement audit as immutable inputs.
- Writes a new batch directory and change-local comparison/review artifacts. It does not modify semantic models, verifier rules, Gold, historical bundles, approved tables, scheduler/backfill, commodity exposure, value-chain output, DCF, or Stage 6.
