## 1. Freeze replay admission

- [x] 1.1 Bind the replay to the archived twenty-report manifest, refined v2 Evidence plan, provider-free preparation/refinement audits, fixed batch identity, Gemini profile, and unchanged provider budgets.
- [x] 1.2 Reject mismatched hashes, report identities, plan/preparation metrics, provider settings, or an existing output directory before the first provider call.

## 2. Implement immutable comparison

- [x] 2.1 Add a strict replay comparison artifact that validates both batch trees and records report-status, accepted/traceable facts, review workload, provider/token/latency, reason-code, source-review, and readiness values and deltas.
- [x] 2.2 Add focused tests proving valid comparison, tamper/incomplete-input rejection, research-only authorization, and no mutation of either batch.

## 3. Execute the one authorized replay

- [x] 3.1 Generate and freeze the v2 provider-free preparation audit; verify cohort/PDF/plan identity, required owners, supported assignments, table context, and traceability before network access.
- [x] 3.2 Perform one read-only Scorpio connectivity/profile preflight, using the authorized external-sandbox path if sandbox DNS or connection behavior fails.
- [x] 3.3 Run exactly one full twenty-report replay as `manufacturing-materials-shadow-refined-gemini-20260910-a` with the fixed Gemini profile and budgets; retain every success/failure without targeted reruns or splicing.

## 4. Review and empirical decision

- [x] 4.1 Generate the source-bound review package, record outcomes for every contracted precision row, and rebuild the readiness audit from those outcomes.
- [x] 4.2 Generate the immutable v1/v2 comparison and a concise summary identifying causal improvements, residual blockers, and whether the gates justify a later restricted-promotion design.
- [x] 4.3 Keep all outputs `accepted_for_review` / research-only with `production_authorization=not_authorized`; do not start Stage 6 or production downstreams.

## 5. Verification and closure

- [x] 5.1 Run focused pytest, scoped Ruff, `git diff --check`, and strict OpenSpec validation.
- [x] 5.2 Review only current-task changes, fix blocking defects, archive the completed change with delta spec and audit artifacts, then commit and push only isolated current-task files.
