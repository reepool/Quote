## 1. Contract and baseline

- [x] 1.1 Record `stage55-policy-four-20260907-a` as the immutable problem baseline and enumerate the three closure scopes without changing historical bundles.
- [x] 1.2 Strict-validate the closure change and confirm production, stage six, legacy backfill, approved tables, commodity exposure, and value-chain publication remain out of scope.

## 2. Satisfied coverage with a supplemental rejection

- [x] 2.1 Add workflow tests proving accepted ranked counterparty facts retain observed coverage and task completion when an extra aggregate Relationship is blocked only as `object_not_allowed`.
- [x] 2.2 Add negative workflow tests proving subject, Evidence, provider, conflict, missing-verify, and other substantive failures remain blocking even when the same field has an accepted record.
- [x] 2.3 Implement the closed resolved-supplemental rejection rule in the existing workflow without accepting or projecting the blocked candidate.

## 3. Explicit consolidation-adjustment subject

- [x] 3.1 Add provider tests for a source-validated `consolidation_adjustment` Segment and Measurement whose only verify failure is `subject_unsupported`.
- [x] 3.2 Add negative provider tests proving ordinary rows, bare `公司`, invalid adjustment labels, and checks with additional reason codes are not normalized to pass.
- [x] 3.3 Add the narrow verifier instruction and deterministic verify-response normalization in the existing Stage 5 provider.

## 4. Offline acceptance

- [x] 4.1 Run the focused workflow/provider tests covering both corrections and fix only current contract blockers.
- [x] 4.2 Run the full company-profile unit suite and Ruff for changed Python files.
- [x] 4.3 Re-run strict OpenSpec validation before any live provider request.
- [x] 4.4 Allow one explicit multi-report scope selection to create the contracted three-scope preflight bundle, while rejecting unknown scopes or selected samples with no matching scope.

## 5. Bounded live validation

- [x] 5.1 Execute one new three-scope preflight containing Ningde segment, Ningde top-five customers, and Putailai segment-and-adjustment using the approved Evidence plan and common LLM gateway.
- [x] 5.2 Verify the preflight completes all three scopes, preserves anonymous ranking facts, excludes the aggregate Relationship, and accepts only source-supported consolidation adjustments.
- [x] 5.3 If 5.2 passes, execute one complete four-report run under another new run ID without reusing preflight or historical records.

## 6. Benchmark and closure decision

- [x] 6.1 Generate the committed-output Gold and real-negative Benchmark plus researcher-readable profiles from the new complete bundle, retaining every failure, conflict, caveat, and unevaluated case.
- [x] 6.2 Confirm all six core chapters are complete for each report, no required execution or frozen §14 blocker remains, and every evaluated real negative passes.
- [x] 6.3 Register `research_slice_usable` only if 6.2 passes; otherwise retain the truthful report/slice status without launching additional targeted runs.
- [x] 6.4 Audit the isolated output root for temporary artifacts and identify exactly one new authoritative complete bundle without deleting historical review evidence.

## 7. Review and handoff

- [x] 7.1 Verify `production_authorization=not_authorized` and confirm no production or stage-six path was invoked or modified.
- [x] 7.2 Review only the closure-change diff, run final targeted validation, and prepare the completed change for archive.
