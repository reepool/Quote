## Why

The two critical and seven noncritical precision errors found in the authoritative twenty-report replay are now closed provider-free, but those bounded regressions do not prove that the corrected path improves real cohort output. One new immutable replay of the same frozen cohort is required to measure execution, source-level precision, and review workload before any restricted production-promotion proposal can be considered.

## What Changes

- Admit exactly one provider-bearing replay of the frozen twenty-report manufacturing/materials cohort using the same manifest/PDF hashes, corrected v3 Evidence plan, Gemini logical profile, token/output/deadline limits, provider-call ceiling, and source-review rule.
- Require the completed nine-error provider-free closure audit as an additional hash-bound admission input and reject changed, missing, or reused inputs before semantic execution.
- Execute through the existing report-local shadow service under one new immutable batch ID, with no targeted rerun, model substitution, candidate splicing, Evidence change, or parameter tuning after the first semantic call.
- Generate a complete source-bound review package, readiness audit, and immutable comparison against `manufacturing-materials-shadow-stability-gemini-20260909-a` covering completion, usability, accepted facts, traceability, provider cost/stability, review workload, precision, and reviewed critical/noncritical errors.
- Close honestly as `ready`, `hold`, or `failed`. Even a passing empirical result authorizes only a separate restricted production-promotion proposal; production and Stage 6 remain closed.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `company-profile-manufacturing-materials-shadow-batch`: add the single-use precision-closure replay admission, immutable comparison, empirical gates, and research-only stop condition.

## Impact

- Affected path: the existing shadow-batch operator, `ManufacturingMaterialsShadowBatchService`, replay admission, source-review, readiness, and comparison helpers.
- Affected artifacts: one new immutable twenty-report batch plus review/readiness/comparison evidence stored without modifying the prior replay or closure audit.
- No semantic vocabulary, Evidence selection, model route, token/deadline budget, Gold expectation, public API, database schema, approved writer, scheduler/backfill, commodity exposure, value-chain publication, DCF, or Stage 6 change.
