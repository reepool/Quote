## 1. Freeze the fresh cohort before provider access

- [x] 1.1 Enumerate every prior company-profile authority, Gold, OOS, canary, shadow,
  targeted, and adjudication sample identity, then select three or four absent local-valid
  manufacturing/materials annual reports with diverse business and disclosure forms.
- [x] 1.2 Freeze one versioned cohort manifest recording issuer, exchange, report title and
  period, PDF path/hash/page count, local-valid evidence, selection rationale, known
  limitations, new immutable batch ID, and `production_authorization=not_authorized`.
- [x] 1.3 Freeze complete six-core-chapter Evidence plans and a provider-free preparation
  audit for every report, including legal-empty/missing-context decisions, supported field
  closure, physical pages, Evidence owners, and all content hashes without expected answers.

## 2. Add the narrow current-contract admission path

- [x] 2.1 Add one fresh-cohort mode/contract to the existing shadow operator and delegate
  report execution and persistence to `ManufacturingMaterialsShadowBatchService` without
  another semantic loop.
- [x] 2.2 Reject cohort/PDF/plan/preparation/field/route/dynamic-token/output-identity or
  production-boundary drift before the first provider request while preserving all prior
  frozen modes and bundles.
- [x] 2.3 Add focused provider-free tests for eligible admission, historical-sample and
  partial-cohort rejection, unsupported fields, output reuse, dynamic-budget forwarding,
  four-model route binding, and unchanged production restrictions.

## 3. Verify the frozen execution contract

- [x] 3.1 Run focused manifest, preparation, Stage 5, provider, projection, bundle, and
  admission tests plus Ruff and strict OpenSpec validation; fix only blockers for this
  fresh-cohort business path.
- [x] 3.2 Validate every bound file/hash and the absence of the output identity, then persist
  a credential-free effective four-model route and dynamic-token-policy receipt without
  modifying or staging the task-start LLM configuration.
- [x] 3.3 Perform one read-only connectivity preflight for configured Scorpio, ZAI, and
  DeepSeek endpoints, using the authorized external path for sandbox-related DNS/network
  failure, without generating semantic candidates.

## 4. Execute the sole fresh-cohort batch

- [x] 4.1 Launch exactly one provider-bearing batch with the frozen identity through the
  current `semantic_extraction` pool and dynamic token policy; after the first semantic
  request, do not tune or replace any contract input and do not target-rerun or splice.
- [x] 4.2 Preserve report-local successes, caveats, holds, typed failures, request/attempt
  lineage, selected token budgets, actual usage, and immutable bundles while confirming
  prior authorities and all production consumers remain unchanged.

## 5. Review, evaluate, and close

- [x] 5.1 Generate a source-bound review package for every blocker, caveat, unresolved item,
  and fixed-defect recurrence candidate plus one accepted or legal-empty result per core
  chapter per report.
- [x] 5.2 Record review outcomes and an empirical cohort audit covering execution, research
  usability, Evidence traceability, model/provider attempts, token behavior, human-review
  workload, and recurrence of all three recently fixed defect families.
- [x] 5.3 Close from observed results without post-hoc Gold or semantic tuning; retain
  `production_authorization=not_authorized` and state only whether this cohort supports a
  later restricted-promotion proposal.
- [x] 5.4 Run final targeted regressions, Ruff, artifact/hash checks, `git diff --check`,
  strict OpenSpec validation, and scoped blocking-defect review; commit and push only this
  change while preserving all task-start workspace changes.
