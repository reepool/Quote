## 1. Freeze the single replay contract

- [x] 1.1 Add one narrow post-stability replay admission bound to the frozen twenty-report manifest, corrected v3 Evidence plan/preparation/correction hashes, completed stability evidence, fixed Gemini profile and budgets, and new immutable batch ID.
- [x] 1.2 Add provider-free tests that admit only the exact contract and reject changed inputs, budgets, route, historical/reused identities, missing stability evidence, or pre-existing output directories before provider execution.

## 2. Validate and execute the cohort once

- [x] 2.1 Run the focused gateway, Stage 5, shadow admission, partition, audit, and operator tests plus scoped Ruff and strict OpenSpec validation; verify the frozen provider-free inputs and hashes.
- [ ] 2.2 Perform one read-only Scorpio/profile preflight, using the authorized external-permission path only if the sandbox shows a likely DNS/network-permission failure, then execute exactly one new-ID twenty-report replay with no targeted rerun or result splice.

## 3. Review and compare the immutable result

- [ ] 3.1 Generate the source-bound review package and complete the frozen review rule for every blocker, caveat, unresolved/adjudication item, and stable first accepted or legal-empty result for each core chapter.
- [ ] 3.2 Generate the readiness audit and immutable before/after comparison with hash-bound execution, usability, traceability, critical-error, review-workload, provider failure, token, latency, precision, and gate metrics.

## 4. Close the research validation

- [ ] 4.1 Record the sole replay honestly as `ready`, `hold`, or `failed`, verify that historical inputs remain unchanged, and document that no second batch, targeted run, model substitution, or contract relaxation occurred.
- [ ] 4.2 Re-run focused tests, Ruff, JSON/hash checks, `git diff --check`, and strict OpenSpec validation; publish the research-only summary with `production_authorization=not_authorized` and all production/Stage 6 paths closed.
