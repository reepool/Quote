## 1. Complete Publication Scope

- [x] 1.1 Add the targeted complete-publication rollout phase and current identity-bound manifests without changing the full-market active phase.
- [x] 1.2 Make explicit expanded instrument backfill with atomic activities select the complete phase and include deterministic downstream families.
- [x] 1.3 Return candidate, verification, promotion, role, exposure-fact, publication, and remaining-gap counts with an explicit execution mode.

## 2. Verification And Queue Stages

- [x] 2.1 Split durable verification from serial publish, inherit old budget configs safely, and migrate unfinished legacy publish items by checkpoint state.
- [x] 2.2 Execute LLM verification targets in bounded concurrent waves with deterministic merge, per-wave persistence, and target-id resume.
- [x] 2.3 Raise the semantic token guard to 50,000 and set semantic/verify concurrency to 20 while keeping publish concurrency at one.

## 3. Reporting

- [x] 3.1 Use actual reconciliation coverage and effective phase in business-profile progress output and identify identity-scoped queue counts.
- [x] 3.2 Select detail notification severity from the result so successful output is not sent as a warning, and distinguish candidate-only from complete/partial publication.

## 4. Verification And Delivery

- [x] 4.1 Add focused runtime, queue migration, async stage, targeted rollout, and scheduler report regressions using fake LLM and temporary databases.
- [x] 4.2 Run focused tests, compile/static checks, and strict OpenSpec validation; review and fix only blocking defects.
- [x] 4.3 Update task status, commit only this change, and push to the configured upstream.
