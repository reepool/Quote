## Why

The sole ownership-aware twenty-report replay was consumed inside a restricted network
namespace and produced no HTTP response or semantic output, while an authorized external
check reached Scorpio immediately. One new-ID replay launched through the external path
is required to evaluate the already-frozen v6 Evidence ownership correction rather than
repeating a known sandbox transport failure.

## What Changes

- Freeze the archived failed replay, the same twenty-report cohort/PDFs, v6 Evidence plan
  and preparation, ownership audits, implementation, Gemini route, `20000/18000` output
  limits, 300-second timeout, 600-call ceiling, review rule, and readiness gates in one
  provider-free recovery proof.
- Add one narrow external-path replay identity to the existing shadow operator; admission
  remains provider-free and delegates to the same report-local semantic service.
- Launch exactly one complete provider-bearing replay outside the sandbox under the
  user's standing `scorpio.reepool.com` transfer authorization, with no semantic or token
  tuning, model substitution, targeted rerun, or historical splicing.
- Produce the contracted source review, readiness, seventeen-scope recurrence, and
  immutable comparison artifacts, then close as `ready`, `hold`, or `failed`.
- Retain `production_authorization=not_authorized` and keep Stage 6 and all production
  consumers closed.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `company-profile-manufacturing-materials-shadow-batch`: permit one transport-recovery
  replay through the authorized external path while freezing every semantic input and
  evaluation gate from the failed ownership replay.

## Impact

- Affects the existing shadow replay admission/operator, focused provider-free tests, one
  new immutable twenty-report batch, and change-local proof/review/readiness/comparison
  artifacts.
- Does not introduce dynamic token budgets, another extraction service, new schema or
  prompt semantics, Gold changes, approved writes, scheduler/backfill, commodity
  exposure, value-chain publication, DCF, or Stage 6.
