## 1. Freeze the external replay contract

- [x] 1.1 Add one narrow new-ID external replay contract/mode that reuses every frozen precision-closure input and budget and cannot reuse the failed sandbox identity.
- [x] 1.2 Add provider-free tests for exact admission, external identity isolation, contract drift, and existing output rejection.

## 2. Execute once through the authorized external path

- [x] 2.1 Run focused tests, Ruff, hashes, output-identity check, and strict OpenSpec validation.
- [ ] 2.2 Launch the sole twenty-report provider-bearing process with external-sandbox permission from the start; retain its result without targeted reruns or tuning.

## 3. Review and measure

- [ ] 3.1 Complete the frozen source review, including recurrence checks for the nine closed errors, or record the exact missing-input precondition if execution fails before reports persist.
- [ ] 3.2 Generate readiness and immutable comparison against `manufacturing-materials-shadow-stability-gemini-20260909-a` when inputs exist, with every contracted metric and gate.

## 4. Close

- [ ] 4.1 Record `ready`, `hold`, or `failed`, verify all historical inputs remain unchanged and every production path remains closed.
- [ ] 4.2 Re-run focused verification, publish the research-only outcome and exact next recommendation, commit, push, and archive the completed change.
