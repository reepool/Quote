## 1. Configuration and Public Contracts

- [x] 1.1 Rename `config/11_llm.json` to `config/13_llm.json`; update every explicit repository reference, configuration template, and deployment document without changing configuration loader behavior.
- [x] 1.2 Define validated non-secret configuration models for logical routes, pools, members, source labels, concrete-profile mappings, weights, borrowing, failover, circuits, and explicit provider resources; reject empty/duplicate names, booleans/fractions/non-positive limits, unsupported strategies, unknown/disabled mappings, label mismatches, capability mismatches, route/profile name collisions, project hard-limit violations, duplicate top-level `llm` owners, and shared-quota resource splitting.
- [x] 1.3 Configure initial disabled/conservative source-suffixed Pipio Grok and Luna concrete profiles using `QUOTE_LLM_PIPIO_GROK_API_KEY` and `QUOTE_LLM_PIPIO_LUNA_API_KEY`, respectively; explicitly retain structured-output, stream, timeout, retry, token, local concurrency/RPM, URL, model, and provider-resource settings, document controlled quota-bucket validation, and do not store key values.
- [x] 1.4 Add a public logical-profile facade for enablement, non-secret description, effective limits, supported capabilities, and deterministic route fingerprint, with a compatible result for unrouted profiles.
- [x] 1.5 Extend public response/error lineage models with logical profile, selected profile, stable source label, route fingerprint, failover count, and redacted attempt records while retaining fixture and direct-profile compatibility.
- [x] 1.6 Add unit tests for every configuration validation branch, both environment-key names, same-URL/different-model profiles, shared and independent quota mappings, logical facade behavior, fingerprint stability, direct-profile fallback, response compatibility, and secret redaction without reading a real `.env`.

## 2. Pool Scheduling and Lifecycle

- [x] 2.1 Implement an injectable process-local `LlmPoolCoordinatorRegistry` keyed by validated pool configuration identity, with explicit snapshot, cleanup, cancellation, and test lifecycle APIs.
- [x] 2.2 Implement bounded pool admission and a hard total logical-execution concurrency cap shared by all logical profiles mapped to the pool.
- [x] 2.3 Implement deterministic weighted deficit round-robin member selection for compatible, enabled, eligible members, with deterministic test hooks and documented bounded fairness tolerance.
- [x] 2.4 Implement optional idle-capacity borrowing while preserving pool and concrete-member limits; expose configured/effective limit and bottleneck, active/wait/oldest-wait, per-source dispatch/ratio/outcome/error-category, circuit/cooldown/probe, failover outcome/category, stage latency/correlation, and provider concurrency/RPM/cooldown in non-secret snapshots.
- [x] 2.5 Keep concrete profile limiter and provider/account coordinator leases at concrete-attempt scope; preserve independent provider `workload_weights` and workload identities, expose the effective minimum across business worker/pool/profile/provider/HTTP limits, and ensure pool activity is neither leaked nor double-counted during retries, repair, cancellation, or failover.
- [x] 2.6 Add fake-clock/fake-transport concurrency tests for shared caps across logical profiles, deterministic 3:1 fairness, low-weight non-starvation in small batches, borrowing, pool/profile/provider/HTTP multilayer limits without deadlock, queue limit, cancellation/deadline/error cleanup, multiple clients sharing one registry, and no permit leaks.
- [x] 2.7 Register/configure the existing `LLM` logger through `config/01_log.json` and `utils/logging_manager.py` task-domain routing; do not add an LLM-specific handler, directory, formatter, or rotation implementation.
- [x] 2.8 Add parameterized LLM routing logs with stable event names: queue/scheduler/lease/retry/repair/deadline/probe/cleanup details at `DEBUG`; admission/source/first-attempt/failover/completion/exhaustion/circuit/lifecycle nodes at `INFO`; recoverable degradation at `WARNING`; terminal configuration/all-source/lifecycle/state-integrity failures at `ERROR`.
- [x] 2.9 Add log-capture tests for event levels, required correlation/source/timing/error fields, exception context, module-level DEBUG switching, task-domain integration, and redaction of keys, headers, document text, prompts/responses, and raw provider bodies.

## 3. Routed Execution, Deadline, and Health

- [x] 3.1 Refactor `LlmClient.complete()` into a stable logical routing entrypoint and a private concrete execution path so concrete calls do not recursively resolve routes or recreate logical request identity.
- [x] 3.2 Introduce a shared absolute logical execution-budget context covering post-admission retries, repair, backoff, failover selection, and concrete attempts; preserve existing initial queue-time semantics and cancellation behavior, and bound each attempt by the minimum concrete-profile/request/remaining-route timeout.
- [x] 3.3 Implement bounded same-source retry/repair followed by classified cross-source failover using `failover.on`, `max_hops`, remaining budget, untried compatible members, and minimum next-attempt budget.
- [x] 3.4 Implement per-member closed/open/half-open circuit behavior, bounded probes, cooldown, recovery, and non-secret metrics separately from provider quota/cooldown management.
- [x] 3.5 Ensure authentication, configuration, cancellation, and deadline failures fail closed by default; allow authentication failover only through explicit configuration plus high-priority operational logging.
- [x] 3.6 Add fake-clock/fake-transport tests for Grok 429 to Luna success, Luna transport timeout to Grok success, parse/schema repair exhaustion then failover, no failover for configuration/cancellation/deadline, authentication policy, no repeat selection of a failed member, one shared deadline, circuit open/half-open recovery, provider-report de-duplication, all-source failure, and terminal safe lineage.
- [x] 3.7 Implement and test two-layer identity: deterministic route fingerprint and stable business runtime/input identity exclude selected source and ephemeral health/counters but include route/output capabilities, actual-attempt identity includes source/model/sequence, route revision changes invalidate reuse, and caller idempotency remains unchanged across retry/failover while request IDs remain per attempt.

## 4. Application Boundary and Source Persistence

- [x] 4.1 Replace direct logical-profile configuration access in `data_manager.py` with the public logical-profile facade for enablement, RPM/effective limits, resolver decisions, lifecycle, resume, and runtime identity.
- [x] 4.2 Replace direct profile access in `research/business_profile_production_rollout.py` and any remaining application, API, scheduler, script, or research module identified by the static scan.
- [x] 4.3 Update business-profile semantic runtime/input identity, checkpoint, artifact, and audit envelopes to use logical profile plus route fingerprint, not a randomly selected concrete model identity.
- [x] 4.4 Propagate public source/route lineage through CNInfo title classification, corporate-action extraction, independent verification, async pipeline/resume, persistence, API/task reporting, and audit queries; preserve historical rows with null or `legacy_unknown` source rather than guessing.
- [x] 4.5 Propagate public source/route lineage through business-profile semantic extraction, structured extraction, verification, async production, rollout, and legacy adapter candidate/audit envelopes without modifying business JSON schemas or evidence gates.
- [x] 4.6 Implement idempotent, repeatable, backward-readable source-lineage database/artifact migration and rollback paths that never overwrite or delete existing analyses and preserve stored lineage when route/pool configuration is rolled back.
- [x] 4.7 Add apply-twice/rollback migration tests for mixed legacy/new records, API/task/audit source reporting tests, and verification that no public gateway path can write approved financial facts directly.

## 5. Existing LLM Business Compatibility Matrix

- [x] 5.1 Run and record a repository-wide static scan for `LlmClient`, `LlmClientProtocol`, `LlmRequest`, `llm_config.profiles`, concrete profile fields, `resource_for_profile`, and `11_llm.json`; classify each result as gateway-internal, migrated application usage, test fixture, or documentation.
- [x] 5.2 Add offline fake-transport regressions for `data_sources/cninfo_announcement_title_llm.py`, covering logical profile use, batch chunking, isolated retry, out-of-order identity, per-event source lineage, failover without cross-wiring, business schema, and applicability validation.
- [x] 5.3 Add offline fake-transport regressions for `data_sources/cninfo_corporate_action_llm.py` and `data_sources/cninfo_corporate_action_pipeline.py`, covering distinct extraction/verifier sources under one `source_event_key`, route-fingerprint identity, completed-result reuse without duplication, deterministic evidence, unchanged promotion gates, persistence, legacy reads, and audit-query lineage.
- [x] 5.4 Add offline fake-transport regressions for `research/business_profile_semantic_extraction.py`, `research/business_profile_semantic_runtime.py`, `research/business_profile_production_rollout.py`, and `research/business_profile_async_production.py`, covering atomic/relationship/structured extraction and verification, route identity, audit/artifact source, checkpoint/resume/rework, promotion manifest/source revision, network/scope/candidate/release gates, and separation of structured-source fallback from LLM failover.
- [x] 5.5 Add legacy-adapter regressions for `research/business_profile_llm.py`, covering synchronous/asynchronous entrypoints, injected fake client, source envelope, shutdown, and absence of independent provider configuration or pool bypass.
- [x] 5.6 Exercise application lifecycle, scheduler, `scripts/research_business_profile_semantic_production.py`, `scripts/dev_validation/validate_common_llm_gateway_live.py`, and `scripts/dev_validation/benchmark_llm_orchestration.py` in a non-network configuration; verify one shared pool/coordinator, `override=False` environment precedence, logical-profile/source reporting, benchmark redaction, and teardown without task/permit/socket/HTTP-client/state leaks.
- [x] 5.7 Confirm all business modules use only stable logical profiles and public gateway/facade APIs; make the static scan a release gate with documented allowed `utils/llm` internal exceptions.

## 6. Validation, Rollout, and Documentation

- [x] 6.1 Run and extend `tests/unit/test_utils/test_llm_gateway.py`, `tests/unit/test_utils/test_llm_orchestration.py`, `tests/unit/test_cninfo_corporate_action_llm.py`, `tests/unit/test_scheduler_cninfo_corporate_action_llm.py`, `tests/unit/test_research/test_business_profile_llm.py`, `tests/unit/test_research/test_business_profile_semantic_extraction.py`, `tests/unit/test_research/test_business_profile_semantic_runtime.py`, `tests/unit/test_research/test_business_profile_async_production.py`, and `tests/unit/test_research/test_scheduler_business_profile_semantic_maintenance.py`, plus all new routed/logging tests, without keys or network access.
- [x] 6.2 Run explicit controlled single-source smoke tests for Grok and Luna against the same synthetic non-sensitive Chinese input, verifying authentication, actual model, streaming/first event, usage/request identifiers, structured output, timeout behavior, and provider-resource quota mapping; keep these tests out of normal unit tests.
- [x] 6.3 Run controlled logical-profile failover plus offline staged-load validation at 10, 25, and 50 logical concurrent requests; record success, 429/5xx, first-event/total latency, dispatch ratio, borrowing, failover, circuit, memory, connection count, shutdown duration, and provider pressure.
- [x] 6.4 Run the provider-backed 10-concurrency stage and enforce its acceptance gate; record the failed 9/10 result, two successful Luna-to-Grok failovers, one Grok deadline exhaustion, provider pressure, resource use, and clean shutdown without proceeding to a higher stage.
- [ ] 6.5 After provider stability is restored, rerun and pass the provider-backed 10-concurrency gate, then run and pass the gated 25- and 50-concurrency stages before declaring live rollout complete.
- [x] 6.6 Confirm live outputs remain candidate-only and pass existing holdout/evidence/promotion gates; document rollout, non-destructive rollback, quota decisions, operational dashboard/alert fields, and the distinction between model-key redundancy and cross-provider disaster recovery.
- [x] 6.7 Run OpenSpec strict validation and repository-required review of the final implementation diff; resolve confirmed issues, rerun focused validation, and record all unchanged baseline worktree modifications separately from this change.
