## 1. Freeze the replay contract

- [x] 1.1 Rebuild the frozen twenty-report Evidence plan and preparation with ownership-
  aware v6, persist both provider-free, and verify their hashes against the archived
  ownership audit.
- [x] 1.2 Create a provider-free proof binding the cohort/PDFs, v6 plan/preparation,
  ownership audit, prior authoritative batch/readiness, current implementation, Gemini
  settings, fixed budgets, source-review rule, readiness gates, new batch ID, absent
  output, and research-only boundary.

## 2. Implement narrow single-use admission

- [x] 2.1 Add one ownership-replay contract/mode to the existing shadow operator and
  delegate to the existing report-local batch service.
- [x] 2.2 Reject manifest/PDF/plan/preparation/audit/implementation/route/budget/batch-ID
  drift and output reuse before provider construction.
- [x] 2.3 Add focused provider-free tests for exact admission, realistic drift classes,
  mode isolation, no pre-admission provider calls, and unchanged production bounds.

## 3. Execute the sole replay

- [x] 3.1 Run focused tests, Ruff, strict OpenSpec validation, hash checks, and output-
  absence checks before network use.
- [x] 3.2 Run one read-only Scorpio/Gemini preflight, repeating only through the
  authorized external path if the sandbox produces a likely DNS/network-permission
  failure, and persist the typed receipt.
- [x] 3.3 Start the sole provider-bearing execution under
  `manufacturing-materials-shadow-operating-ownership-gemini-20260911-a`; after the
  first complete persisted report produced eight uniform 300-second pre-HTTP DNS
  failures, stop the globally blocked process and preserve the immutable report and
  interrupted-attempt evidence without tuning, replacement, substitution, or splicing.

## 4. Quantify and review the result

- [x] 4.1 Record that the contracted six-chapter source review is unavailable because
  the failed transport produced zero semantic responses, zero accepted/legal-empty
  rows, and no complete batch manifest; make no favorable precision claim.
- [x] 4.2 Generate failed empirical-readiness, operating-scope recurrence, and immutable
  comparison artifacts bound to the persisted report and prior batch, with unavailable
  metrics explicit rather than reconstructed.

## 5. Close and verify

- [x] 5.1 Record `ready`, `hold`, or `failed` from the frozen gates and retain
  `production_authorization=not_authorized`, fixed token budgets, Stage 6 closure, and
  all production-consumer restrictions.
- [x] 5.2 Run targeted regressions, Ruff, artifact/hash checks, `git diff --check`, and
  strict OpenSpec validation; review blocking defects, then commit and push only this
  isolated change without touching pre-existing workspace files.
