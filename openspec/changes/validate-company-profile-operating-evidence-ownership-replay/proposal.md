## Why

The Evidence-role twenty-report replay completed transport but left seventeen operating-
quantity scopes with schema failures caused by false page-by-field ownership. The v6
ownership correction has provider-free causal proof; one controlled full-cohort replay
is now required to measure whether it improves real semantic yield and review workload.

## What Changes

- Freeze the existing twenty-report cohort/PDF identities, the ownership-aware v6
  Evidence plan and preparation audit, the prior authoritative Evidence-role batch,
  the ownership correction audit, Gemini route, provider budgets, readiness gates, new
  output identity, and research-only boundary in one admission proof.
- Add one narrow replay mode to the existing shadow operator. It validates all frozen
  inputs and output absence before provider construction and delegates to the existing
  report-local batch service.
- Run one read-only Scorpio preflight and exactly one complete provider-bearing replay,
  preserving every report-local success or typed failure without parameter tuning,
  targeted reruns, model substitution, or historical result splicing.
- Produce source-bound review, empirical readiness, operating-scope recurrence, and
  immutable before/after comparison artifacts from the new batch.
- Close honestly as `ready`, `hold`, or `failed` while retaining
  `production_authorization=not_authorized` and keeping Stage 6 and all production
  consumers closed.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `company-profile-manufacturing-materials-shadow-batch`: admit one ownership-aware v6
  full-cohort replay and require source-bound quantitative evaluation before any
  restricted production-promotion proposal.

## Impact

- Affects the existing shadow replay admission/operator, focused admission tests, one
  immutable twenty-report result, and change-local proof/review/readiness/comparison
  artifacts.
- Uses the existing `ManufacturingMaterialsShadowBatchService` and Scorpio Gemini route;
  no new semantic loop, model, prompt/schema relaxation, dynamic token policy, timeout
  change, Gold change, database write, approved table, scheduler/backfill, commodity
  exposure, value-chain publication, DCF, or Stage 6 path is introduced.
