## Why

The last complete twenty-report shadow replay achieved 100% execution, Evidence
traceability, and sampled precision, but all reports remained `hold` because the
segment chapter still contained annual-period reconciliation and incomplete-heading
failures. Those two observed failure classes are now repaired and externally validated
under new identities, so one current-implementation replay is needed to measure whether
the frozen cohort is broadly research-usable without weakening semantic safeguards.

## What Changes

- Freeze the existing twenty-report manifest, PDFs, v5 Evidence plan and prepared
  scopes, the September 11 segment-retry baseline, the two segment repair closeouts,
  current implementation hashes, Gemini profile, budgets, review rule, readiness gates,
  and research-only boundary as one replay contract.
- Add one new immutable batch identity and a narrow operator mode that validates every
  bound input and output absence before provider creation, then delegates to the existing
  report-local shadow service.
- Execute the frozen cohort exactly once through the user-authorized external Scorpio
  path, preserving every report-local success and typed failure without tuning, targeted
  reruns, model substitution, historical splicing, or bundle mutation.
- Generate the complete source-review, readiness, recurrence, and immutable comparison
  artifacts from the new batch and close honestly as `ready`, `hold`, or `failed`.
- Keep `production_authorization=not_authorized`; do not open Stage 6 or any production
  writer or downstream publication path.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `company-profile-manufacturing-materials-shadow-batch`: admit exactly one replay of
  the frozen cohort under the current segment heading/period implementation and require
  source-bound empirical readiness evidence before any production-promotion proposal.

## Impact

- Affected implementation: one replay contract/mode in
  `scripts/run_company_profile_shadow_batch.py` and directly related provider-free
  admission tests; the existing service remains the execution owner.
- Affected artifacts: one immutable twenty-report batch plus change-local admission,
  preflight, source-review, readiness, recurrence, comparison, and closeout records.
- No cohort, PDF, Evidence, prompt, schema, model, parameter, Gold, public API, database,
  approved table, scheduler/backfill, commodity exposure, value-chain, DCF, or Stage 6
  change.
