## 1. Freeze current replay inputs

- [x] 1.1 Validate the corrected full twenty-report Evidence plan and generate a complete provider-free preparation audit with zero provider calls.
- [x] 1.2 Freeze the new batch ID, September 11 baseline, both external segment repair results, current relevant implementation hashes, Gemini profile, budgets, readiness gates, output path, and research-only boundary in a hash-bound admission proof.

## 2. Implement narrow replay admission

- [x] 2.1 Add one segment-heading replay contract/mode to the existing shadow operator and delegate execution to the existing report-local service without a second semantic loop.
- [x] 2.2 Reject plan, audit, implementation, repair-result, route, budget, batch-ID, and output-reuse drift before provider creation.
- [x] 2.3 Add focused provider-free tests for exact admission, each drift class, mode isolation, corrected-plan preparation, and unchanged production boundaries.

## 3. Execute the sole full-cohort replay

- [x] 3.1 Run focused tests, Ruff, strict OpenSpec validation, hash checks, and output-absence checks before network use.
- [x] 3.2 Run one read-only Scorpio/Gemini preflight through the authorized external path and persist its typed result.
- [x] 3.3 Execute the frozen twenty-report cohort exactly once under the new batch identity, preserving every report-local success or typed failure without tuning, targeted reruns, substitution, or splicing.

## 4. Produce source-bound evaluation

- [x] 4.1 Generate and complete the six-core-chapter source review for the new batch, including every blocker/caveat/unresolved row and the stable accepted/legal-empty samples.
- [x] 4.2 Generate readiness, segment-recurrence, and immutable before/after comparison artifacts bound to both batch identities and all inputs by content hash.

## 5. Close and verify

- [x] 5.1 Record `ready`, `hold`, or `failed` from the frozen gates and retain `production_authorization=not_authorized` with Stage 6 and every production consumer closed.
- [ ] 5.2 Run targeted regression tests, Ruff, artifact/hash checks, `git diff --check`, and strict OpenSpec validation; review only blocking defects, then commit and push isolated task changes without touching pre-existing workspace files.
