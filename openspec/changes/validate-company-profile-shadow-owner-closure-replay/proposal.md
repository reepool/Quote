## Why

The provider-free owner regressions are closed, but the immutable external twenty-report replay still reflects the old Evidence plan and remains `hold`. One controlled replay using a newly generated plan is required to measure whether those fixes improve cohort usability and review workload under the existing production-readiness gates.

## What Changes

- Freeze a new Evidence plan and provider-free preparation audit for the same twenty reports and PDFs using the current planner.
- Prove before any provider call that all twenty reports prepare successfully, the seven closed owner/substance errors do not recur, and the valid `920033.BJ` material legal-empty control remains owner-bound.
- Admit one new immutable Gemini replay identity with the existing model profile, token, deadline, call-budget, source-review, readiness, and comparison rules.
- Execute the full cohort once through the existing report-local service, then publish source review, readiness, and comparison against the corrected external baseline.
- Close honestly as `ready`, `hold`, or `failed`; production remains `not_authorized` unless a later restricted promotion change is separately approved.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `company-profile-manufacturing-materials-shadow-batch`: require a current-planner owner-closure Evidence plan, provider-free recurrence checks, and one immutable empirical replay before production readiness can be reconsidered.

## Impact

- Affected implementation: the existing shadow Evidence plan version, one narrow replay contract/mode, and directly related tests.
- Affected artifacts: one new plan/preparation audit and one new batch-local review/readiness result plus immutable comparison audit.
- No prompt, model, token/deadline, Gold, public API, database, approved writer, scheduler/backfill, commodity exposure, value-chain publication, DCF, or Stage 6 change.
