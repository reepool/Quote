## 1. Baseline and sample freeze

- [x] 1.1 Record `stage55-closure-four-20260907-a` and the offline Gold 18/24 evaluation as immutable in-sample references, and confirm that neither is an input record source for the new run.
- [x] 1.2 Select exactly one official local manufacturing/materials annual report absent from the four-report manifest, Gold, targeted runs, and adjudication ledger; record its business-model and disclosure-form selection rationale.
- [x] 1.3 Freeze a versioned out-of-sample manifest with sample/report identity, exchange, report period, PDF path and hash, known limitations, and `production_authorization=not_authorized` before semantic execution.

## 2. Six-chapter Evidence plan

- [ ] 2.1 Prepare bounded source-native Evidence for overview plus Activity and for segment performance, preserving continuous physical pages, headers, units, footnotes, continuation links, and legal-empty behavior.
- [ ] 2.2 Prepare the same bounded Evidence for operating quantity/capacity, material/energy input, counterparty/concentration, and business regime event or explicit `not_applicable`.
- [ ] 2.3 Audit the frozen plan against all six core chapters before any model call; record unreadable, incomplete, or legally empty scopes without adding Gold-shaped answers or new PDF/parser capability.

## 3. Validation-manifest execution path

- [ ] 3.1 Add focused tests proving the explicit one-sample validation manifest is admitted while the existing four-report mode still rejects extra reports, mixed manifests, unknown kinds, and post-output sample replacement.
- [ ] 3.2 Implement the minimum manifest discrimination needed to call the existing preparation, semantic service, provider, projection, and atomic bundle owners for the frozen sample.
- [ ] 3.3 Keep the operator entry thin and prove it does not duplicate sample iteration, extract/repair/verify, disposition, projection, benchmark, or persistence loops.

## 4. Offline verification before live execution

- [ ] 4.1 Run focused manifest, preparation, service, provider, projection, acceptance-policy, and bundle tests; fix only blockers required for the one-sample validation path.
- [ ] 4.2 Run Ruff for changed Python files and strict OpenSpec validation, and confirm the frozen four-report authority and historical bundle hashes remain unchanged.
- [ ] 4.3 Verify no production database, approved table, scheduler, API, Telegram, legacy backfill, CommodityExposure, ValueChainRole, DCF, or stage-six state is reachable from the validation command.

## 5. One bounded out-of-sample run

- [ ] 5.1 Execute exactly one operator-level complete run for the frozen sample under a new run ID using the existing common LLM gateway and bounded attempt/failover policy.
- [ ] 5.2 Confirm the committed bundle or bounded failure manifest contains only this run's sample, Evidence, calls, candidates, dispositions, coverage, projection, and status, with no historical, Gold, fixture, diagnostic, or interrupted record spliced in.
- [ ] 5.3 Do not launch a targeted run or second complete run; retain any typed preparation, transport, schema, verification, or semantic failure as an out-of-sample finding.

## 6. Generalization evaluation and human review

- [ ] 6.1 Calculate the report's `usable`, `usable_with_caveats`, `hold`, or `failed` state from the current six-chapter completion, frozen blockers, verification, coverage, subject-use restrictions, fixture guards, and triggered real negative cases without creating post-hoc Gold.
- [ ] 6.2 Generate one researcher-readable report covering every chapter's accepted facts, legal empty results, caveats, blockers, and restricted uses, with physical Evidence references.
- [ ] 6.3 Apply the pre-frozen deterministic audit to every blocker/caveat/adjudication and the first accepted or legal-empty result in stable request order for each core chapter; record source text, page, Evidence, semantic support, and usage restriction.
- [ ] 6.4 For every item requiring human adjudication, list the exact original text or table cells, one-based physical page, Evidence identity, runtime target, conflicting rule, alternatives, and recommended decision.
- [ ] 6.5 Classify each finding as existing guard behavior, preparation gap, new disclosure form, adapter/verifier mismatch, execution failure, or unresolved policy decision; do not implement new semantic rules in this change.

## 7. Closure and handoff

- [ ] 7.1 Audit the isolated output for temporary artifacts and verify all outputs retain `accepted_for_review` semantics and `production_authorization=not_authorized`.
- [ ] 7.2 State whether this one sample supports bounded reuse of the existing contract, or remains hold/failed, without claiming industry-wide or production readiness.
- [ ] 7.3 Update only the directly affected current documentation, review the scoped diff, run final targeted validation, and prepare the completed change for independent review and archive.
