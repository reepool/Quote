## Why

The authoritative twenty-report shadow replay completed all reports with complete
Evidence traceability and no critical reviewed semantic error, but remained `hold`
because context-only Evidence was still used as if it owned requested fields. The
provider contract now exposes explicit Evidence ownership roles, so one controlled
full-cohort replay is required to measure the actual effect before any production
promotion work is considered.

## What Changes

- Freeze the existing twenty-report cohort/PDFs, corrected v5 Evidence plan and
  provider-free preparation audit, the September 11 authoritative replay/readiness
  baseline, the Evidence-role implementation/audit, Gemini route, provider budgets,
  readiness gates, output path, and research-only boundary in one hash-bound proof.
- Add one narrow `evidence-role-semantic-replay` operator mode and immutable batch
  identity that validate all bound inputs and output absence before provider creation,
  then delegate to the existing report-local shadow service.
- Perform one read-only Scorpio preflight and execute the complete frozen cohort exactly
  once, preserving every report-local success or typed failure without tuning, targeted
  reruns, model substitution, candidate splicing, or historical mutation.
- Produce the complete source-review, readiness, reason-code recurrence, and immutable
  before/after comparison artifacts from the new batch and close honestly as `ready`,
  `hold`, or `failed`.
- Keep `production_authorization=not_authorized`; do not open Stage 6 or any approved,
  scheduler/backfill, commodity-exposure, value-chain, or DCF path.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `company-profile-manufacturing-materials-shadow-batch`: admit exactly one full-cohort
  replay after the Evidence-role request-contract fix and require source-bound empirical
  evaluation before a separate restricted production-promotion proposal.

## Impact

- Affected implementation: one replay contract/mode in
  `scripts/run_company_profile_shadow_batch.py`, one bounded proof validator in the
  existing shadow service, and focused provider-free admission tests. The existing
  `ManufacturingMaterialsShadowBatchService` remains the sole execution owner.
- Affected artifacts: one immutable twenty-report batch plus change-local proof,
  admission, preflight, source-review, readiness, recurrence, comparison, and closeout
  records.
- Authorized external transfer is limited to the frozen Evidence and request content
  required by this replay through `scorpio.reepool.com`; no cohort, PDF, Evidence plan,
  prompt rule, schema, validator, model, parameter, Gold, public API, database, or
  production consumer changes are included.
