# AGENTS.md

> 本文件定义 Quote 量化研究项目的项目级规则。
>
> 本文件与用户级 `~/.codex/AGENTS.md` 叠加使用。
> 通用的“业务优先、最小充分实现、防止过度工程、Git 安全和 Review 范围控制”以用户级规则为基础，本文件只补充 Quote 项目特有约束。

---

## 1. 项目定位

本项目是一个使用 Python 开发的量化研究系统，主要服务于投资研究、数据处理、技术分析、因子研究、回测、组合与风险分析。

你在本项目中需要理解专业投资与量化研究语义，包括：

- 股票；
- 指数；
- 期货；
- 期权；
- 基金；
- 债券；
- 大宗商品；
- 宏观分析；
- 行业研究；
- 公司研究；
- 技术分析；
- 量化交易；
- 组合管理；
- 风险控制；
- 回测评估。

**项目背景用于帮助做出正确的局部实现决策，不代表每个任务都需要建设完整的量化基础设施。**

默认：

- 优先服务 A 股和港股量化研究；
- 数据源优先兼容免费公开来源；
- 代码优先本地运行；
- 回测优先日频；
- 研究逻辑优先可解释；
- 结果应可验证、可复现；
- 投资判断保持谨慎，不夸大策略有效性。

---

## 2. Python 与项目技术约定

### 2.1 Python

- 优先遵循项目当前 Python 版本和现有依赖；
- 无更具体约束时使用 Python 3.10+ 可用语法；
- 优先使用类型注解；
- 重要函数写清晰 docstring；
- 函数职责尽量单一；
- 避免全局可变状态；
- 避免在 import 阶段下载数据、执行耗时逻辑或写文件。

按现有项目技术栈优先使用：

- `pathlib.Path`：路径；
- `pandas` / `polars`：表格数据；
- `numpy`：数值计算；
- `dataclasses` / `pydantic`：结构化对象；
- `logging`：日志；
- `pytest`：测试；
- `ruff` / `black` / `mypy`：若项目已有配置则遵循。

### 2.2 依赖与环境

优先检查并遵循：

- `pyproject.toml`
- `requirements.txt`
- `uv.lock`
- `poetry.lock`

不要随意升级核心依赖。

部分 npm / nvm CLI 位于：

```bash
/home/python/.nvm/versions/node/v24.7.0/bin
```

如需调用 `openspec`，可以临时：

```bash
export PATH="/home/python/.nvm/versions/node/v24.7.0/bin:$PATH"
```

或直接使用相应绝对路径。

不要为此永久修改用户 shell 配置文件，除非任务明确要求。

---

## 3. 数据获取与接口

### 3.1 数据范围

项目可能处理：

- 股票、指数和行业行情；
- 期货、期权；
- 宏观数据；
- 财务和估值；
- 公司公告与公司行动；
- 基金、债券；
- 大宗商品及相关产业数据。

可使用的数据源包括但不限于：

- AkShare；
- BaoStock；
- yfinance；
- 交易所公开数据；
- 东方财富；
- 同花顺；
- 巨潮资讯。

这只是允许范围，不代表当前任务必须增加多个数据源。

### 3.2 数据源封装

优先通过项目已有的数据接口、client 或 repository 访问第三方数据。

如果已有统一接口，优先复用。

只有当当前业务路径确实无法合理使用现有接口时，才新增最窄必要封装。

不要因为一个新功能就主动统一整个项目的数据源架构。

不要在研究逻辑中无必要地散落第三方 API URL 和供应商特定细节。

---

## 4. 数据质量与时间语义

> 本节属于**当前功能涉及时必须遵守的正确性约束**，不是默认需要建设的数据治理系统。

处理行情、财务、指数、行业、宏观等数据时，根据当前功能实际使用的字段和路径检查：

- 日期是否为正确交易日；
- 时间戳是否存在时区问题；
- 股票代码和交易所后缀是否统一；
- 使用前复权、后复权还是不复权；
- 财务数据的报告期、公告日和实际可得日；
- 指数成分和行业分类是否存在历史变更；
- 停牌、退市、新股、ST、北交所、科创板、创业板等特殊状态；
- 数据源是否有缺漏、延迟、限流或字段语义不明；
- 关键记录是否重复；
- 必需字段是否缺失；
- 类型和数值是否明显异常。

只实现当前业务路径所需的必要校验。

除非需求明确要求，不要自动建设：

- 全局数据血缘系统；
- provenance framework；
- 多阶段 validation pipeline；
- 多重数据 gate；
- schema registry；
- 全平台数据质量评分；
- 通用审计平台。

发现与当前结果无关的潜在数据治理问题，可以记录为后续建议。

---

## 5. 回测与投资逻辑正确性

### 5.1 必须避免

涉及策略、信号、组合或回测时，必须防止：

- 未来函数；
- 数据泄露；
- 幸存者偏差；
- 使用当时尚不可得的数据；
- 使用完整样本拟合参数后回测整个历史；
- 不合理忽略手续费和滑点；
- 不合理忽略流动性；
- 不合理忽略停牌、涨跌停和退市。

### 5.2 时间点

必须根据当前任务明确区分：

- 数据产生时点；
- 数据实际可得时点；
- 信号生成时点；
- 交易执行时点；
- 收益归因时点。

财务和公司事件数据尤其需要关注“报告期”和“市场实际知道该信息的日期”之间的区别。

### 5.3 交易假设

如果当前任务需要交易假设但需求未明确，应采用简单、透明、保守并可说明的默认假设。

不要为了一个局部策略功能而主动扩建完整交易仿真平台。

---

## 6. 因子研究

当任务实际涉及因子研究时，根据需求检查：

- 因子方向；
- 计算窗口；
- 滞后期；
- 缺失值处理；
- 极值处理；
- 标准化；
- 行业或市值中性化；
- IC；
- Rank IC；
- 分组收益；
- 换手率；
- 衰减。

不要因为新增一个因子计算就默认实现完整因子评估流水线。

因子结果必须能说明其**可用时点**，避免未来数据污染。

---

## 7. 数据存储、缓存与输出

项目可能使用：

- CSV；
- Parquet；
- SQLite；
- DuckDB；
- PostgreSQL。

根据现有项目结构区分原始数据、清洗数据、特征数据、回测结果和报告输出。

不得在无提示情况下：

- 覆盖重要原始数据；
- 清空数据库；
- 删除历史缓存；
- 覆盖研究结果；
- 改变已有数据格式或目录结构。

对慢速、限流或不稳定接口，如果当前功能确实需要缓存，优先复用已有缓存方式。

不要因单一功能建设新的全局缓存平台。

---

## 8. 日志与错误处理

### 8.1 日志

使用 `logging`，库代码避免大量 `print`。

对于耗时流程，在真正有助于定位进度和故障的关键阶段记录日志。

根据当前任务可记录：

- 数据源；
- 请求参数或范围；
- 行数；
- 缓存路径；
- 阶段；
- 关键耗时；
- 异常原因。

不要为了日志完整性引入新的 observability framework。

### 8.2 错误处理

- 用户输入错误应给出明确错误；
- 文件不存在应明确处理；
- 网络请求失败应有与当前路径匹配的处理；
- 数据为空应区分合法为空和异常为空；
- 不使用裸 `except:`；
- 不静默返回空 DataFrame 掩盖真实错误，除非函数契约明确允许。

---

## 9. 测试中的金融语义

在通用测试规则之外，涉及金融逻辑时，测试不仅验证代码能运行，还应验证当前任务相关的金融含义，例如：

- 调仓日是否正确；
- 信号是否按正确时点使用；
- 财务数据是否按实际可得日使用；
- 收益率计算是否符合定义；
- 复权处理是否符合预期；
- 股票池是否动态生成；
- 手续费和滑点是否按需求生效；
- 最大回撤、年化收益、夏普等指标定义是否正确。

测试优先使用小型、可控、内联或 fixture 数据。

普通单元测试不要依赖实时网络数据。

如确有必要测试外部数据源，尽量标记为集成测试。

---

## 10. OpenSpec 与长程功能开发

项目中的复杂功能可能先通过需求文档和 OpenSpec change 定义。

对于 OpenSpec change：

1. 将 change 中的 MUST / required 行为提炼为明确业务验收条件；
2. 优先按 vertical slice 实现；
3. 先打通最小业务闭环；
4. 再完成核心业务规则；
5. 再处理现实异常和边界；
6. 再做测试与回归；
7. 最后才做必要清理和 Review。

推荐阶段：

```text
Milestone 1：最小业务闭环
Milestone 2：完整核心业务规则
Milestone 3：必要异常与现实边界
Milestone 4：测试与回归验证
Milestone 5：必要清理、Review、提交
```

不要默认采用：

```text
通用框架
→ 数据治理
→ 多级校验
→ 服务抽象
→ 平台能力
→ 最后才实现业务逻辑
```

### 10.1 business-first-goal Skill

用户级 Skill 预期位于：

```text
~/.agents/skills/business-first-goal/SKILL.md
```

对于：

- `/goal`；
- OpenSpec 长程开发；
- 多阶段功能实现；
- 已出现 scope drift 的开发；

优先显式使用：

```text
$business-first-goal
```

如果 Goal 中没有显式调用，但 Codex 根据 Skill description 自动匹配，也应按该工作流执行。

---

## 11. 本仓库 Git 闭环授权

本节仅适用于 Quote 仓库。

如果当前任务实际新增或修改了功能代码、测试、配置或配套文档，且用户当次没有明确要求：

- “只修改、不提交”；
- “不要提交”；
- “不要推送”；
- 只分析 / 诊断 / Review；

则用户对**当前任务中 Codex 本人产生且能够与任务开始基线可靠隔离的改动**，持续授权执行正常 commit 和普通 push。

### 11.1 开始开发前

运行：

```bash
git status --short --branch
```

记录当前：

- 分支；
- 暂存区；
- 工作区；
- 未跟踪文件。

任务开始前已有内容视为“他人/既有改动”，严格遵守用户级 AGENTS.md 的保护规则。

### 11.2 完成功能后

先运行与改动范围匹配的：

- 测试；
- 最小业务验证；
- 必要静态检查；
- 必要格式化检查。

然后对未提交改动执行 Review，可使用：

```bash
codex review --uncommitted
```

或交互式 `/review`。

按照用户级 AGENTS.md 的 A/B/C 分类处理 Review。

不得为了让 Review 零建议而扩大任务范围。

### 11.3 只提交本人改动

验证和 Review 通过后：

1. 再次对照开始时 Git 基线；
2. 只暂存当前任务本人改动；
3. 检查：

```bash
git diff --cached --stat
git diff --cached
```

4. 使用符合仓库风格的提交信息创建新提交；
5. 不使用 `--amend`、rebase 或 force push，除非用户明确要求；
6. 将当前分支正常推送到已配置上游；
7. 若没有上游，仅在远程仓库和目标分支无歧义时：

```bash
git push -u origin HEAD
```

如果无法可靠隔离本人改动，停止提交并向用户说明。

推送失败时保留本地提交并报告原因，不通过强推解决。

### 11.4 最终报告

最终回复必须说明：

- 完成的业务功能；
- Review 发现及分类；
- 实际修复内容；
- 验证命令和结果；
- 提交哈希；
- 推送结果；
- 是否仍有任务开始前的既有改动留在工作区，并明确这些内容未被触碰。

---

## 12. 研究结果表达

投资或研究输出应区分：

- 数据事实；
- 计算结果；
- 策略假设；
- 主观判断；
- 风险与限制。

不要把回测结果等同于未来收益。

如果数据字段、金融定义或数据可得时点不确定，应明确说明并优先通过项目代码、原始文档或样例数据验证，而不是默认合理。

---

## 13. Quote 项目最终检查

完成任务前，除用户级通用检查外，再确认：

1. 是否引入未来函数、数据泄露或幸存者偏差？
2. 时间字段和数据可得时点是否正确？
3. 复权、交易日、代码格式等口径是否与当前功能一致？
4. 是否为了数据质量“理论完整性”建设了当前业务不需要的基础设施？
5. 是否保持现有接口、缓存和数据格式兼容，或已明确说明变化？
6. 是否只提交和推送了当前任务本人改动？

满足业务验收和必要正确性后结束任务，不继续寻找新的平台化建设方向。
