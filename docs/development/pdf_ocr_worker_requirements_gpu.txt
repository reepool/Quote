# Install in a CUDA 11.8 virtual environment outside Quote conda.
# The host must expose a supported NVIDIA device before starting this worker.
paddlepaddle-gpu==3.3.1
paddleocr==3.7.0
pypillow>=10.0
numpy>=1.26,<3
